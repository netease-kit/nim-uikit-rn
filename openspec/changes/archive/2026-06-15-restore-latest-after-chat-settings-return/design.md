## Overview

这次只修正聊天页在临时失焦再返回时的最新消息展示与手动滚动衔接逻辑，不改消息列表的基础倒序渲染模式。

## Design

- 保留现有“x条新消息”和 deferred 最新消息提示逻辑
- 当用户从聊天设置等临时外页返回后，如果此时仍停留在最新消息位置且存在 deferred 最新消息，第一次手动拖动列表时释放这些最新消息
- 这样既保留新消息提示，又不会让暂停展示状态继续阻塞用户手动滑动查看新消息

## Risks

- 聊天页同时存在前后台切换和外页跳转两套临时离开场景，需避免把前后台恢复到底的逻辑误套用到设置页返回场景，导致错误清掉新消息提示
