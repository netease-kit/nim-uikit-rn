## MODIFIED Requirements

### Requirement: Conversation Preview Copy

The app SHALL show the required preview copy for location messages in the conversation list.

#### Scenario: Location message preview

- **WHEN** the last message of a conversation is a location message
- **THEN** the conversation list MUST display `[位置消息]` as the preview text
