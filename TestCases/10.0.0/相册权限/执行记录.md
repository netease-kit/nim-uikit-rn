# 相册权限测试用例执行记录

## 执行范围

- 用例目录：`TestCases/10.0.0/相册权限`
- 已执行范围：`0851` 至 `0855`
- 执行方式：按 `AGENTS.md` 要求一次只验证一条用例；当前环境无 Detox/Maestro 等自动化 runner，采用“读取用例 -> 核对实现路径 -> 必要代码修复 -> targeted lint/类型检查/OpenSpec 校验 -> 记录结果”的顺序。
- 环境限制：本套件依赖 iOS/Android 真机或可交互模拟器、系统相册权限弹窗、系统照片选择器和 limited photos 系统补选界面。当前终端只能验证代码链路、配置和启动前提，无法直接完成系统权限弹窗与系统相册 UI 的实机操作。

## 验证命令基线

- `npx eslint utils/permissions.ts 'app/chat/[id].tsx'`：通过。
- `npx tsc --noEmit`：通过。
- `OPENSPEC_TELEMETRY=0 openspec validate align-chat-album-permission-flow --type change --no-interactive`：通过。
- `npm run android`：通过。2026-05-15 已在连接的 Android 真机 `PKB110` 上完成 `expo run:android` 构建、安装 APK、启动 Metro，并自动打开 development build。

## 用例执行明细

| 用例                           | 结果                                           | 执行记录                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------ | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0851 首次从相册选择图片/视频   | 通过（修复后代码通过，系统弹窗需复测）         | 已修复聊天页 `app/chat/[id].tsx` 的 `图片` 工具栏入口：点击后不再先弹业务自定义“选择媒体/原图原视频”对话框，而是直接进入 `ensureMediaLibraryPermission()` 的系统相册权限链路；首次未授权时由 `expo-image-picker` 触发系统相册权限弹窗，满足用例要求的“首次点击直接出现系统权限请求”。运行时阻塞：当前终端无法直接拉起系统权限弹窗确认高版本“三选项/低版本二选项”具体 UI。                                                                                                      |
| 0852 系统权限请求弹窗拒绝      | 通过（修复后代码通过，二次点击提示需复测）     | 已修复 `utils/permissions.ts`：首次未授权点击图片入口时，仅调用系统 `requestMediaLibraryPermissionsAsync()`，拒绝后直接返回并停留聊天页；再次点击时，先通过 `getMediaLibraryPermissionsAsync()` 发现状态已为 `denied`，再弹出“权限不足 / 请在设置页面添加相册权限”，并提供“去设置”动作调用 `Linking.openSettings()`。这与用例“先拒绝，再次点击提示去设置中打开权限”的顺序一致。                                                                                                |
| 0853 系统权限请求弹窗允许全部  | 通过（代码通过，系统相册范围需复测）           | 当前图片入口在权限已允许全部时，会直接调用 `ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images', 'videos'], allowsMultipleSelection: true, selectionLimit: 9 })` 打开系统相册；首次授权成功后同一次点击链路会继续打开系统相册，再次点击也会直接打开系统相册，满足用例预期。运行时阻塞：当前终端无法实机确认系统相册展示全部照片。                                                                                                                                      |
| 0854 系统权限请求弹窗-允许部分 | 通过（修复后代码通过，limited 可见范围需复测） | 当前相册权限允许 `accessPrivileges === 'limited'` 视为可用，随后直接进入系统照片选择器；limited 模式下照片可见范围由系统控制，业务代码不会绕过系统限制。已新增 limited 状态下的业务提示：“去选择”可直接进入当前已授权照片选择器，“添加更多照片”会调用 `MediaLibrary.presentPermissionsPickerAsync(['photo', 'video'])` 进入系统补选照片流程，覆盖备注中“可在设置中再次添加更多照片”。运行时阻塞：当前终端无法确认系统 limited picker 中仅展示已勾选 3 张照片这一系统 UI 结果。 |
| 0855 权限变更                  | 通过（修复后代码通过，系统切换行为需复测）     | 已修复权限变更后的恢复链路：1）允许全部/允许部分改为不允许后，再次点击图片入口会命中 `denied` 分支并提示去设置打开权限；2）不允许/允许部分改为允许全部后，图片入口会直接打开系统相册；3）不允许/允许全部改为允许部分后，图片入口会走 limited 选择链路，并支持“添加更多照片”；4）允许部分变更后再次补选更多照片时，当前实现通过 `MediaLibrary.presentPermissionsPickerAsync(...)` 调起系统补选界面，具体“之前添加是否保留”仍由系统决定，符合用例备注“系统行为以系统而定”。      |
