# 单聊-群聊设置页执行记录

## 2026-05-17 12:13:34 CST

- 执行范围: 用户本轮指定的 38 条冒烟用例。
- 执行策略: 按用户补充要求改为风险分层执行；确定性 UI/业务逻辑以用例正文 + 代码路径核验为准，只对登录态、启动态和不确定运行环境用 iOS 模拟器抽验。
- iOS 抽验: iPhone 16 Pro iOS 18.2 已安装并启动 App；账号 `15988864334` 已处于登录态，实际登录 accid 为 `25799`；会话列表可见单聊、讨论组、群聊数据。
- 本轮新增修复:
  - `app/team/member-picker.tsx`: 成员邀请页候选包含好友和数字人，空态/搜索空态文案从“好友”改为“成员”，避免数字人场景下文案不准确。
  - `openspec/changes/align-selection-surface-contact-scope/tasks.md`: 补充成员邀请页通用“成员”空态文案要求。
- 沿用前序修复:
  - `0356-群禁言`: `app/team/settings.tsx` 已在普通群群主视角直接展示 `群禁言` 开关。
  - Web SSR 启动问题: `app/team/manage.tsx`、`src/NEUIKit/common/utils/constants.ts` 已去除 Web-only SDK 间接加载。

### 本轮验证命令

- `npx prettier --check app/team/member-picker.tsx openspec/changes/align-selection-surface-contact-scope/tasks.md app/team/settings.tsx app/team/manage.tsx src/NEUIKit/common/utils/constants.ts TestCases/10.0.0/单聊-群聊设置页/执行记录.md`: 通过。
- `OPENSPEC_TELEMETRY=0 openspec validate expose-team-chat-banned-switch --type change --no-interactive`: 通过。
- `OPENSPEC_TELEMETRY=0 openspec validate align-selection-surface-contact-scope --type change --no-interactive`: 通过。
- `npx tsc --noEmit`: 通过。
- `npm run lint`: 通过。
- `CI=1 npx expo start --port 8085 --clear`: Metro ready；`curl -I http://localhost:8085` 返回 `HTTP/1.1 200 OK`。
- 已知环境问题: Expo 依赖版本仍有既有漂移提示，例如 `expo@55.0.8` 期望 `~55.0.24`、`react-native@0.83.2` 期望 `0.83.6`，本轮未调整依赖。

### 本轮 38 条结果

| 用例                                        | 结果                            | 记录                                                                                                                                                                                                               |
| ------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `0316-单聊设置邀请好友及数字人加入群聊`     | CODE-PASS / IOS-SMOKE           | 单聊设置加号进入 `/conversation/picker`，排除自己、当前单聊对象和黑名单好友，候选包含好友与数字人；创建讨论组随机默认头像、群名按成员昵称 30 字符省略。iOS 已确认账号有实际单聊/群聊数据；未破坏性创建真实讨论组。 |
| `0320-开启消息提醒`                         | CODE-PASS                       | 单聊开关打开调用 `conversationStore.toggleMute(conversationId, false)`，语义为取消免打扰/开启提醒；实际系统通知到达需对端发消息，不在本轮模拟器抽验范围。                                                          |
| `0322-聊天置顶开`                           | CODE-PASS                       | 单聊置顶打开走云会话 `stickTopConversationActive(..., true)` 或本地回退，列表排序使用 `stickTop`。                                                                                                                 |
| `0324-创建讨论组`                           | CODE-PASS                       | 与 `0316` 同链路；创建成功后 `router.replace('/chat/[id]')` 进入新讨论组聊天页。未创建真实群以避免污染账号数据。                                                                                                   |
| `0329-+添加按钮`                            | FIXED / CODE-PASS               | 讨论组设置页加号进入 `/team/member-picker`；候选过滤已有成员、自己、黑名单好友并包含数字人。本轮修复空态文案为“可邀请成员”。                                                                                       |
| `0331-点击成员头像`                         | CODE-PASS                       | 讨论组设置页成员头像点击跳转 `/friend/friend-card` 并传 `accountId`。                                                                                                                                              |
| `0334-开启消息提醒`                         | CODE-PASS                       | 讨论组设置页提醒开关打开调用 `conversationStore.toggleMute(conversationId, false)`；实际提醒到达需真实新消息。                                                                                                     |
| `0336-聊天置顶开`                           | CODE-PASS                       | 讨论组置顶打开走云会话或本地置顶回退，会话列表按置顶优先排序。                                                                                                                                                     |
| `0339-讨论组最后一个非数字人成员退出讨论组` | CODE-PASS / RUNTIME-BLOCKED     | 客户端退出/成员事件刷新逻辑存在；“最后非数字人成员退出后自动解散、数字人不能做群主”是服务端多人+数字人规则，需 B 账号和数字人成员实测。                                                                            |
| `0344-退出讨论组`                           | CODE-PASS / DESTRUCTIVE-SKIPPED | 退出讨论组有二次确认，确认后 `leaveTeam()`、删除会话并回会话列表；本轮未对现有真实讨论组执行退出。                                                                                                                 |
| `0351-群成员左侧"+"`                        | FIXED / CODE-PASS               | 群设置页按权限/成员上限展示加号并进入 `/team/member-picker`；候选包含好友和数字人，空态文案已改为“成员”；无权限不展示。                                                                                            |
| `0352-群成员头像`                           | CODE-PASS                       | 群设置页成员头像点击跳转好友名片。                                                                                                                                                                                 |
| `0353-开启消息提醒-开关`                    | CODE-PASS                       | 群提醒开关打开/关闭映射 `mute=false/true`，会话列表 muted 展示免打扰图标；实际通知需新消息验证。                                                                                                                   |
| `0354-聊天置顶`                             | CODE-PASS                       | 群置顶开关打开/关闭走云会话或本地回退，列表按 `stickTop` 展示/取消置顶。                                                                                                                                           |
| `0356-群禁言`                               | FIXED / CODE-PASS               | 群主主设置页直接展示 `群禁言` 开关，调用 `teamStore.setChatBannedMode()`；聊天页已有禁言态禁用输入和更多入口。                                                                                                     |
| `0357-群禁言@数字人`                        | CODE-PASS / RUNTIME-BLOCKED     | 客户端支持群禁言和数字人候选；“群内数字人回复失败、非群成员数字人正常回复”依赖服务端数字人响应规则，需真实消息收发。                                                                                               |
| `0363-解散群聊`                             | CODE-PASS / DESTRUCTIVE-SKIPPED | 群主确认后 `dismissTeam()`、删除会话并回列表；多端弹窗和群数据抹除需真实多人群，未对现有群执行解散。                                                                                                               |
| `0364-退出群聊`                             | CODE-PASS / RUNTIME-BLOCKED     | 非群主确认后 `leaveTeam()`、删除会话并回列表；当前仅有一个登录账号，未验证普通成员视角真实退出。                                                                                                                   |
| `0380-搜索群成员`                           | CODE-PASS                       | 成员列表按所见名称 `getUIKitAppellation({ account, teamId })` 搜索，排序为群主、管理员、普通成员加入时间正序。                                                                                                     |
| `0370-点击群成员头像`                       | CODE-PASS                       | 点击自己进入 `/user/my-detail`，点击他人进入 `/friend/friend-card`。                                                                                                                                               |
| `0371-群成员列表成员昵称展示顺序`           | CODE-PASS                       | `getUIKitAppellation()` 优先 im-store UIKit 命名；本地回退为好友备注 > 群昵称 > 个人昵称 > 消息昵称 > accid。                                                                                                      |
| `0372-查看群成员头像`                       | CODE-PASS                       | `UIKitUserAvatar` 头像优先显式头像/用户头像，默认头像 label 不取群昵称，符合用例规则。                                                                                                                             |
| `0373-群成员列表排序`                       | CODE-PASS                       | 成员列表排序代码为群主第一、管理员第二、普通成员按 `joinTime` 正序。                                                                                                                                               |
| `0382-群主进入群成员列表`                   | CODE-PASS                       | 群主 `canManage=true`，除自己/群主外显示移除按钮。真实群主数据未做破坏性移除。                                                                                                                                     |
| `0383-管理员进入群成员列表`                 | CODE-PASS / RUNTIME-BLOCKED     | 管理员可管理普通成员，不能移除群主和管理员；需要管理员账号视角实测。                                                                                                                                               |
| `0385-群管理员移除群成员`                   | CODE-PASS / RUNTIME-BLOCKED     | 管理员移除普通成员走二次确认、网络检查、`kickMembers()`；需管理员+普通成员真实群。                                                                                                                                 |
| `0386-移除群成员`                           | CODE-PASS / DESTRUCTIVE-SKIPPED | 群主/管理员移除成员会刷新成员和群信息；本轮未移除现有真实群成员。                                                                                                                                                  |
| `0389-移除数字人`                           | CODE-PASS / RUNTIME-BLOCKED     | 移除逻辑按 `accountId` 处理，不区分数字人；需真实数字人成员在群中。                                                                                                                                                |
| `0390-群主从群成员列表移除管理员`           | CODE-PASS / RUNTIME-BLOCKED     | 群主可移除管理员，移除后刷新成员；再次加群为普通成员属服务端角色规则，需真实管理员成员。                                                                                                                           |
| `0408-拍摄头相上传`                         | CODE-PASS / RUNTIME-BLOCKED     | 拍摄走相机权限、`launchCameraAsync({ allowsEditing: true })`，保存本地 URI 上传头像；需系统相机/裁剪器实测。                                                                                                       |
| `0409-从相册选择选择头相上传`               | CODE-PASS / RUNTIME-BLOCKED     | 相册走权限、`launchImageLibraryAsync({ allowsEditing: true })`，保存本地 URI 上传头像；需系统相册/裁剪器实测。                                                                                                     |
| `0411-切换选择默认图标`                     | CODE-PASS                       | 头像页展示 `DEFAULT_TEAM_AVATARS`，点击切换 `avatarUri` 并显示选中态。                                                                                                                                             |
| `0412-上传头像后保存`                       | CODE-PASS / RUNTIME-BLOCKED     | 默认头像直接更新，拍摄/相册本地 URI 走上传后更新；真实上传结果需有可编辑群执行保存。                                                                                                                               |
| `0426-修改群名称保存`                       | CODE-PASS                       | 群名称必填，空值保存按钮置灰；保存调用 `updateTeamInfo({ name })` 并返回设置页。                                                                                                                                   |
| `0440-保存群介绍`                           | CODE-PASS                       | 群介绍保存调用 `updateTeamInfo({ intro })`，成功返回设置页；不改保存写回同值。                                                                                                                                     |
| `0450-设置我在群里的昵称`                   | CODE-PASS / RUNTIME-BLOCKED     | 保存调用 `updateMyNick()` 并重新加载成员；多端接收端展示需其他账号实测。                                                                                                                                           |
| `0451-删除我在群里的昵称`                   | CODE-PASS / RUNTIME-BLOCKED     | 清空保存调用 `updateMyNick(teamId, '')`，显示名回退个人昵称/备注；接收端展示需其他账号实测。                                                                                                                       |
| `0458-设置我在群里的昵称后查看群成员列表`   | CODE-PASS / RUNTIME-BLOCKED     | 保存后成员列表使用 `getUIKitAppellation({ account, teamId })` 展示本人群昵称；他人视角需其他账号实测。                                                                                                             |

## 2026-05-17 11:56:47 CST

- 执行范围: 用户指定的 38 条冒烟用例，覆盖单聊设置、讨论组设置、群设置、群成员列表、移除成员、头像/名称/介绍/群昵称设置。
- 执行方式: 串行读取用例正文 + 终端静态核验 + 代码路径核验 + Expo Web 启动验证。未进行真机/模拟器手动点击。
- 执行结论: 代码路径核验通过；修复 2 个问题；真实通过判定仍受运行时条件阻塞。

### 本次修复

- `0356-群禁言`: 修复群设置页缺少直接“群禁言”开关的问题。
  - 新增 OpenSpec change: `expose-team-chat-banned-switch`。
  - `app/team/settings.tsx` 在普通群群主视角直接展示 `群禁言` 开关，调用 `teamStore.setChatBannedMode()`，聊天页已有禁言态会随群资料刷新生效。
- Web 启动验证暴露 SSR 兼容问题并已修复。
  - `app/team/manage.tsx` 不再为一个字符串 key 引入 H5 UIKit 常量模块，避免间接加载 Web-only SDK。
  - `src/NEUIKit/common/utils/constants.ts` 移除对 `nim-web-sdk-ng/dist/esm/nim` 的直接 import，使用稳定消息类型数值生成 `REPLY_MSG_TYPE_MAP`，解决 `self is not defined` 导致 Web HTTP 500 的问题。

### 已执行验证

- `OPENSPEC_TELEMETRY=0 openspec validate expose-team-chat-banned-switch --type change --no-interactive`: 通过。
- `npx tsc --noEmit`: 通过。
- `npm run lint`: 通过。
- `npx expo install --check`: 失败，现有 Expo SDK 依赖版本与推荐版本不一致，包括 `expo@55.0.8` 期望 `~55.0.24`、`react-native@0.83.2` 期望 `0.83.6` 等。
- `CI=1 npx expo start --port 8084 --clear`: Metro 启动成功；请求 `http://localhost:8084` 返回 `HTTP/1.1 200 OK`。
  - 备注: 默认 `npm run start` 因 8081 已有同项目 Expo 进程且 CI 模式不能交互选择端口而跳过，本次使用显式 8084 验证。

### 运行时阻塞条件

- 当前终端环境没有可交互真机/模拟器，无法实际点击页面、系统弹窗、相机/相册裁剪器。
- 多数用例依赖真实云信登录态、好友 A/B/F、数字人 C/D、群主/管理员/普通成员身份、多人群/讨论组、多端接收端、服务端群事件和通知消息，当前终端无法判定真实业务通过。
- 因此本次结果以 `CODE-PASS` / `FIXED + CODE-PASS` / `RUNTIME-BLOCKED` 记录，不将需真机和真实账号链路的用例标记为完全通过。

### 单条执行进度

| 用例                                        | 结果                                | 记录                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0316-单聊设置邀请好友及数字人加入群聊`     | CODE-PASS / RUNTIME-BLOCKED         | 核验 `app/session/p2p-settings.tsx` 加号进入 `/conversation/picker`；选择器排除自己、当前单聊成员和黑名单好友，合并好友与数字人；创建时 `TeamStore.createTeam()` 随机默认头像，讨论组群名按成员昵称生成并限制 30 字符，创建后进入新群聊。运行时缺少真实好友/数字人和设备点击。                                                                                 |
| `0320-开启消息提醒`                         | CODE-PASS / RUNTIME-BLOCKED         | 单聊设置页“开启消息提醒”开关值为 `!conversation?.mute`，打开时调用 `conversationStore.toggleMute(conversationId, false)`，对单聊落到取消免打扰。运行时缺少真实消息通知验证。                                                                                                                                                                                   |
| `0322-聊天置顶开`                           | CODE-PASS / RUNTIME-BLOCKED         | 单聊设置页打开“聊天置顶”调用云会话 `stickTopConversationActive(conversationId, true)` 或本地回退 `toggleStickTop(..., true)`；会话列表按置顶优先排序并显示置顶标识。运行时缺少设备点击和会话列表观察。                                                                                                                                                         |
| `0324-创建讨论组`                           | CODE-PASS / RUNTIME-BLOCKED         | 单聊设置创建讨论组链路与 `0316` 一致，创建成功后进入新群聊页；讨论组标记通过 team serverExtension 保存。运行时缺少真实好友、数字人和服务端创建验证。                                                                                                                                                                                                           |
| `0329-+添加按钮`                            | CODE-PASS / RUNTIME-BLOCKED         | 讨论组设置页成员横排和顶部加号进入 `/team/member-picker`；邀请页过滤已有成员、自己和黑名单好友，成功后调用 `teamStore.inviteMembers()` 并刷新成员。运行时缺少真实讨论组和设备点击。                                                                                                                                                                            |
| `0331-点击成员头像`                         | CODE-PASS / RUNTIME-BLOCKED         | 讨论组设置页成员头像 Pressable 跳转 `/friend/friend-card` 并传入成员 `accountId`。运行时缺少设备点击和真实成员。                                                                                                                                                                                                                                               |
| `0334-开启消息提醒`                         | CODE-PASS / RUNTIME-BLOCKED         | 讨论组设置页消息提醒开关打开时调用 `conversationStore.toggleMute(conversationId, false)`；TEAM 会话走 `setTeamMessageMuteMode(...OFF)`。运行时缺少真实群消息提醒验证。                                                                                                                                                                                         |
| `0336-聊天置顶开`                           | CODE-PASS / RUNTIME-BLOCKED         | 讨论组设置页打开“聊天置顶”走云会话置顶或本地置顶回退；会话列表排序使用 `stickTop`。运行时缺少设备点击和列表观察。                                                                                                                                                                                                                                              |
| `0339-讨论组最后一个非数字人成员退出讨论组` | CODE-PASS / RUNTIME-BLOCKED         | 退出/解散事件监听存在：`app/_layout.tsx`、聊天页和设置页会刷新群资料/成员。该用例核心是服务端“最后非数字人成员退出后自动解散、数字人不当群主”规则，必须真实多人和数字人配置验证。                                                                                                                                                                              |
| `0344-退出讨论组`                           | CODE-PASS / RUNTIME-BLOCKED         | 讨论组底部动作为“退出讨论组”，二次确认后执行 `ensureNetworkAvailable()`、`teamStore.leaveTeam()`、删除会话并返回会话列表；取消停留当前页。运行时缺少真实讨论组退出验证。                                                                                                                                                                                       |
| `0351-群成员左侧"+"`                        | CODE-PASS / RUNTIME-BLOCKED         | 普通群设置页按成员上限、角色和邀请权限展示加号；点击进入 `/team/member-picker`，邀请前检查网络和成员上限。运行时缺少真实群角色和点击验证。                                                                                                                                                                                                                     |
| `0352-群成员头像`                           | CODE-PASS / RUNTIME-BLOCKED         | 普通群设置页成员头像使用 UIKit 身份展示适配，点击跳转好友名片。运行时缺少设备点击和真实成员头像观察。                                                                                                                                                                                                                                                          |
| `0353-开启消息提醒-开关`                    | CODE-PASS / RUNTIME-BLOCKED         | 群设置页消息提醒开关打开/关闭分别映射 `mute=false/true`；会话列表 muted 会显示免打扰标识。运行时缺少真实新消息和会话列表观察。                                                                                                                                                                                                                                 |
| `0354-聊天置顶`                             | CODE-PASS / RUNTIME-BLOCKED         | 群设置页置顶开关打开/关闭走云会话或本地回退，`ConversationStore` 排序和会话行标识支持置顶。运行时缺少设备点击和列表排序观察。                                                                                                                                                                                                                                  |
| `0356-群禁言`                               | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：普通群群主在 `app/team/settings.tsx` 主设置页可直接看到并操作“群禁言”开关；开关调用 `teamStore.setChatBannedMode()`。聊天页已有 `isCurrentTeamChatBanned`，普通成员被禁言时输入框展示“当前群主设置为禁言”，文本、语音、表情、图片、更多面板均禁用，群主/管理员不受禁言。验证通过：OpenSpec、tsc、lint、Web HTTP 200。运行时缺少群主/普通成员双端实测。 |
| `0357-群禁言@数字人`                        | CODE-PASS / RUNTIME-BLOCKED         | 群禁言模式设置和聊天页禁言态已存在；数字人列表/选择器接入 `imStoreV2Bridge.aiUsers`。该用例要求已入群数字人回复失败、非群成员数字人正常回复，属于服务端数字人响应规则，必须真实数字人配置和消息收发验证。                                                                                                                                                      |
| `0363-解散群聊`                             | CODE-PASS / RUNTIME-BLOCKED         | 群主底部动作为“解散群聊”，确认后执行 `teamStore.dismissTeam()`、删除会话并返回 `/(tabs)`；聊天页监听 `onTeamDismissed` 弹出“该群聊已解散”。运行时缺少真实多人群和多端事件验证。                                                                                                                                                                                |
| `0364-退出群聊`                             | CODE-PASS / RUNTIME-BLOCKED         | 非群主底部动作为“退出群聊”，确认后执行 `teamStore.leaveTeam()`、删除会话并返回会话列表。运行时缺少真实普通成员退出验证。                                                                                                                                                                                                                                       |
| `0380-搜索群成员`                           | CODE-PASS / RUNTIME-BLOCKED         | 群成员列表搜索使用 `getUIKitAppellation({ account, teamId })` 的所见名称匹配；排序为群主、管理员、普通成员，再按 `joinTime` 正序。运行时缺少真实昵称/备注/群昵称数据输入验证。                                                                                                                                                                                 |
| `0370-点击群成员头像`                       | CODE-PASS / RUNTIME-BLOCKED         | 成员列表头像/信息行点击自己进入 `/user/my-detail`，点击他人进入 `/friend/friend-card`。运行时缺少设备点击验证。                                                                                                                                                                                                                                                |
| `0371-群成员列表成员昵称展示顺序`           | CODE-PASS / RUNTIME-BLOCKED         | 昵称展示走 `getUIKitAppellation`，优先级为好友备注、群昵称、用户资料昵称/消息昵称、账号；与用例要求一致。运行时缺少真实多昵称数据观察。                                                                                                                                                                                                                        |
| `0372-查看群成员头像`                       | CODE-PASS / RUNTIME-BLOCKED         | 成员列表头像使用 `UIKitUserAvatar`，显式好友头像、im-store 用户头像、本地用户头像和稳定色块兜底。运行时缺少真实头像数据截图验证。                                                                                                                                                                                                                              |
| `0373-群成员列表排序`                       | CODE-PASS / RUNTIME-BLOCKED         | 成员列表排序代码明确为群主优先、管理员其次、普通成员按 `joinTime` 正序。运行时缺少真实成员加入时间和角色数据验证。                                                                                                                                                                                                                                             |
| `0382-群主进入群成员列表`                   | CODE-PASS / RUNTIME-BLOCKED         | 群主 `canManage=true`，成员列表右上角展示“邀请”，可对非自己、非群主成员显示移除按钮。运行时缺少群主账号验证。                                                                                                                                                                                                                                                  |
| `0383-管理员进入群成员列表`                 | CODE-PASS / RUNTIME-BLOCKED         | 管理员 `canManage=true`，可进入成员列表并显示管理能力；代码禁止管理员移除群主和其他管理员。运行时缺少管理员账号验证。                                                                                                                                                                                                                                          |
| `0385-群管理员移除群成员`                   | CODE-PASS / RUNTIME-BLOCKED         | 管理员对普通成员显示“移除”，确认后检查网络并调用 `teamStore.kickMembers()`；权限不足错误映射“您暂无操作权限”。运行时缺少真实管理员/普通成员验证。                                                                                                                                                                                                              |
| `0386-移除群成员`                           | CODE-PASS / RUNTIME-BLOCKED         | 群主/管理员移除成员走二次确认、网络检查、`kickMember`、成员和群信息刷新；成员已不存在错误会刷新成员列表。运行时缺少真实群成员移除验证。                                                                                                                                                                                                                        |
| `0389-移除数字人`                           | CODE-PASS / RUNTIME-BLOCKED         | 移除逻辑按 accountId 处理，不区分好友和数字人；数字人若在成员列表中且操作者有权限，会走同一 `kickMembers` 路径。运行时缺少真实数字人成员验证。                                                                                                                                                                                                                 |
| `0390-群主从群成员列表移除管理员`           | CODE-PASS / RUNTIME-BLOCKED         | 群主 `canManage=true` 且代码仅禁止移除群主本人，未禁止群主移除管理员；确认后调用 `kickMembers()`。运行时缺少真实群主/管理员验证。                                                                                                                                                                                                                              |
| `0408-拍摄头相上传`                         | CODE-PASS / RUNTIME-BLOCKED         | 群头像编辑页“拍摄”调用 `ensureCameraPermission()` 和 `ImagePicker.launchCameraAsync({ allowsEditing: true })`，保存本地 uri 时调用 `teamStore.updateTeamAvatar()` 上传并更新群头像。运行时缺少系统相机和裁剪器。                                                                                                                                               |
| `0409-从相册选择选择头相上传`               | CODE-PASS / RUNTIME-BLOCKED         | “从相册选择”调用 `ensureMediaLibraryPermission()` 和 `ImagePicker.launchImageLibraryAsync({ allowsEditing: true })`，保存时上传并更新头像。运行时缺少系统相册和裁剪器。                                                                                                                                                                                        |
| `0411-切换选择默认图标`                     | CODE-PASS / RUNTIME-BLOCKED         | 头像页展示 `DEFAULT_TEAM_AVATARS` 默认头像网格，点击设置 `avatarUri` 并显示选中态。运行时缺少设备点击截图验证。                                                                                                                                                                                                                                                |
| `0412-上传头像后保存`                       | CODE-PASS / RUNTIME-BLOCKED         | 保存头像前检查网络；本地 uri 走存储上传后更新群资料，远程默认头像直接 `updateTeamInfo({ avatar })`；成功后返回上一页。运行时缺少真实上传和服务端群资料验证。                                                                                                                                                                                                   |
| `0426-修改群名称保存`                       | CODE-PASS / RUNTIME-BLOCKED         | 群名称编辑页非空保存调用 `teamStore.updateTeamInfo({ name: value.trim() })` 并返回；空名称保存按钮置灰。运行时缺少真实群资料保存验证。                                                                                                                                                                                                                         |
| `0440-保存群介绍`                           | CODE-PASS / RUNTIME-BLOCKED         | 群介绍编辑页保存调用 `teamStore.updateTeamInfo({ intro: value.trim() })` 并返回设置页；设置页绑定 `team?.intro` 展示。运行时缺少真实群资料保存验证。                                                                                                                                                                                                           |
| `0450-设置我在群里的昵称`                   | CODE-PASS / RUNTIME-BLOCKED         | 群昵称编辑页保存调用 `teamStore.updateMyNick()` 并重新加载成员；昵称展示通过 `getUIKitAppellation`，好友备注优先，其次群昵称。运行时缺少真实多端消息和成员列表观察。                                                                                                                                                                                           |
| `0451-删除我在群里的昵称`                   | CODE-PASS / RUNTIME-BLOCKED         | 清空群昵称保存调用 `teamStore.updateMyNick(teamId, '')`；展示名回退到个人昵称，好友备注仍优先。运行时缺少真实群昵称删除验证。                                                                                                                                                                                                                                  |
| `0458-设置我在群里的昵称后查看群成员列表`   | CODE-PASS / RUNTIME-BLOCKED         | 保存群昵称后 `loadMembers()` 刷新成员；成员列表使用 `getUIKitAppellation({ account, teamId })`，自己和他人视角均按备注/群昵称优先级展示。运行时缺少本人和他人双端成员列表验证。                                                                                                                                                                                |

### 未执行用例

- 本次用户指定范围内无跳过；所有 38 条均已串行读取、核验并记录。

### 后续要求

- 要完成真实冒烟通过判定，需要准备可交互 Android/iOS 设备或模拟器、真实 IM 登录账号、好友 A/B/F、数字人 C/D、群主/管理员/普通成员角色、多端登录环境，以及相机/相册权限。
- 依赖对齐问题仍存在：需后续决定是否按 `npx expo install --check` 建议升级 Expo SDK 55 相关包。

## 2026-05-15 09:39:32 CST

- 执行范围: `0316` - `0459`，共 144 条用例。
- 执行方式: 终端静态/启动验证 + 代码路径核验。未进行真机手动操作。
- 执行结论: 阻塞，无法在当前终端环境完成该用例集的真实通过判定。

### 已执行检查

- `npx tsc --noEmit`: 通过。
- `npm run lint`: 失败。
  - 已修复本用例集相关的 `app/team/member-picker.tsx` Prettier 格式问题。
  - 剩余失败位于其他模块: `app/(tabs)/my.tsx`、`app/chat/media-viewer.tsx`、`app/user/*` 等格式或导入排序问题。
- `npm run start -- --non-interactive`: Expo 开始启动，但当前 Expo CLI 提示 `--non-interactive is not supported, use $CI=1 instead`，本次终端没有输出 Metro ready 状态，未确认启动成功。

### 代码覆盖核验

- 单聊设置入口与页面: `app/chat/[id].tsx` -> `app/session/p2p-settings.tsx`。
- 群聊设置入口与页面: `app/chat/[id].tsx` -> `app/team/settings.tsx`。
- 群成员列表: `app/team/members.tsx`。
- 群成员邀请: `app/team/member-picker.tsx`。
- 群名称、群介绍、群昵称、群头像编辑: `app/team/edit.tsx`。
- 群组状态与成员操作: `stores/TeamStore.ts`。

### 阻塞原因

- 用例集大量依赖真实 IM 登录账号、好友关系、数字人配置、群聊/讨论组数据、群主/管理员/普通成员角色、多端登录、断网/重连/网络切换、iOS/Android 系统权限弹窗、拍照/相册能力，当前纯终端环境无法执行这些交互。
- 当前仓库 lint 未通过，剩余失败位于其他模块，按仓库验证规则不能标记为通过。
- Expo 启动未在本次终端输出 ready 状态，不能作为应用可运行通过依据。

### 单条执行进度

| 用例                                                             | 结果                                | 记录                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ----------------------------------------------------------------------------------------------------- |
| `0316-单聊设置邀请好友及数字人加入群聊`                          | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/session/p2p-settings.tsx` 单聊设置加号进入 `/conversation/picker` 并传入 `seedAccountId`；`app/conversation/picker.tsx` 标题为“选择”，排除自己、当前单聊成员和黑名单好友，接入 `imStoreV2Bridge.aiUsers` 并触发 `getAIUserListActive()`；创建时 `inviteeIds` 自动包含当前单聊成员，`TeamStore.createTeam()` 随机选择默认群头像，群名按成员昵称拼接并超过 30 字符省略。运行时阻塞：当前终端无真实登录态、好友 A/B/F、数字人 C/D 和可交互设备，无法实际点击创建并检查群成员列表。                                                                                                                                                                                                                                                           |
| `0317-更多`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/chat/[id].tsx` 右上角非多选态展示 `UIKitIcon type="icon-More"`；单聊会话类型点击后跳转 `/session/p2p-settings` 并传入 `conversationId`；`app/session/p2p-settings.tsx` 设置页面标题为“聊天设置”。运行时阻塞：当前终端无可交互设备，无法实际点击右上角更多并截图确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0318-单聊设置页面UI`                                            | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/session/p2p-settings.tsx` 使用 Stack 默认返回入口，标题为“聊天设置”；顶部展示好友头像、昵称和加号；设置项包含“标记”“开启消息提醒”“聊天置顶”。运行时阻塞：当前终端无可交互设备，无法进入页面做视觉截图核验。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0319-标记`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/session/p2p-settings.tsx` 的“标记”行点击后跳转 `/chat/pins` 并传入 `conversationId`；`app/chat/pins.tsx` 页面标题为“标记消息”，会调用 `messageStore.loadPinnedMessages()` 加载当前会话标记消息。运行时阻塞：当前终端无可交互设备，无法实际点击并确认页面展示。                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0320-开启消息提醒`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/session/p2p-settings.tsx` 中“开启消息提醒”开关值为 `!conversation?.mute`；打开开关时 `handleToggleMute(true)` 调用 `conversationStore.toggleMute(conversationId, false)`；`stores/ConversationStore.ts` 对单聊调用 `V2NIMSettingService.setP2PMessageMuteMode(...OFF)`，语义为取消免打扰/开启提醒。运行时阻塞：当前终端无可交互设备和对端好友消息，无法验证系统通知/提醒实际到达。                                                                                                                                                                                                                                                                                                                                                        |
| `0321-关闭消息提醒`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：关闭“开启消息提醒”时 `handleToggleMute(false)` 调用 `conversationStore.toggleMute(conversationId, true)`；`stores/ConversationStore.ts` 对单聊调用 `setP2PMessageMuteMode(...ON)`，语义为开启免打扰；会话列表 `app/(tabs)/index.tsx` 将 `item.mute` 映射为 `muted`，`UIKitConversationRow` 在 muted 时展示 `icon-xiaoximiandarao`。运行时阻塞：当前终端无可交互设备和对端好友消息，无法验证实际无提醒。                                                                                                                                                                                                                                                                                                                                        |
| `0322-聊天置顶开`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/session/p2p-settings.tsx` 中“聊天置顶”开关值为 `!!conversation?.stickTop`；打开时调用云会话 `stickTopConversationActive(conversationId, true)`，无云会话 store 时回退 `conversationStore.toggleStickTop(conversationId, true)`；`ConversationStore` 排序时置顶会话优先，会话列表将 `stickTop` 传给 `UIKitConversationRow` 并展示 `icon-xiaoxizhiding`。运行时阻塞：当前终端无可交互设备，无法实际打开开关并观察会话列表排序。                                                                                                                                                                                                                                                                                                             |
| `0323-聊天置顶关`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：关闭“聊天置顶”时 `handleToggleStickTop(false)` 调用云会话 `stickTopConversationActive(conversationId, false)` 或本地 `conversationStore.toggleStickTop(conversationId, false)`；刷新会话后 `stickTop=false` 不再参与置顶优先排序，会话行不展示置顶图标。运行时阻塞：当前终端无可交互设备，无法实际关闭开关并观察会话列表取消置顶。                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0324-创建讨论组`                                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复通知文案：`utils/teamNotification.ts` 的 TEAM_INVITE 通知从“成员加入群组”调整为“邀请者邀请成员加入群聊”，并补充 `align-team-notification-message-copy` OpenSpec 场景。代码核验通过：单聊设置页加号进入标题为“选择”的 `/conversation/picker`；选择页排除黑名单好友并创建高级群/讨论组等价会话；`TeamStore.createTeam()` 随机生成默认头像；群名取成员昵称前 30 字符并省略；创建成功后进入新群聊页。验证：`npx prettier --check utils/teamNotification.ts openspec/changes/align-team-notification-message-copy/specs/team-conversation-notifications/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy` 通过。运行时阻塞：当前终端无真实账号、好友和可交互设备，无法实际确认被邀请成员会话列表新增讨论组。 |
| `0325-更多`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/chat/[id].tsx` 右上角更多在 `conversationType === TEAM` 时跳转 `/team/settings`，并传入 `teamId` 与 `conversationId`；`app/team/settings.tsx` 为讨论组/群聊设置页。运行时阻塞：当前终端无可交互设备，无法实际点击右上角更多确认跳转。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0326-讨论组设置页面UI`                                          | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复讨论组设置文案：新增 `align-discussion-settings-copy` OpenSpec；`app/team/settings.tsx` 将名称/头像/底部动作调整为“讨论组名称”“讨论组图像”“退出讨论组”，底部确认执行退出讨论组。代码核验通过：页面标题“设置”，展示讨论组头像和名称、群成员行和数量、添加按钮、成员头像、“标记”“历史记录”“开启消息提醒”“聊天置顶”“退出讨论组”。验证：`npx prettier --check app/team/settings.tsx ...` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备，无法实际截图核验 UI。                                                                                                                                                                                                                   |
| `0327-讨论组名称>`                                               | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 展示 `讨论组名称` 行，点击后跳转 `/team/edit`，参数为 `field=name`、`title=讨论组名称`，进入讨论组信息设置/名称编辑页。运行时阻塞：当前终端无可交互设备，无法实际点击确认跳转。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0328-讨论组设置页展示群号`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 在讨论组名称下方展示 `群号：{resolvedTeamId                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |     | '-'}`，位置符合“展示在讨论组名称下”的预期。运行时阻塞：当前终端无可交互设备，无法实际截图核验。       |
| `0329-+添加按钮`                                                 | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复成员邀请页标题：`app/team/member-picker.tsx` 从“创建群组”改为“选择”。代码核验通过：讨论组设置页顶部和成员横排均有加号入口，点击进入 `/team/member-picker`；成员选择页过滤已在群成员、自己和黑名单好友，选择后调用 `teamStore.inviteMembers()` 并 `router.back()` 回到设置页；`TeamStore.inviteMembers()` 邀请成功后重新加载成员。验证：`npx prettier --check app/team/member-picker.tsx app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无真实群和可交互设备，无法实际邀请好友并观察人数更新。                                                                                                                                                                                                                               |
| `0330-讨论组成员`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 的“群成员”行显示成员数量和箭头，点击跳转 `/team/members` 并传入 `teamId`；`app/team/members.tsx` 加载并展示该讨论组成员列表。运行时阻塞：当前终端无可交互设备，无法实际点击确认跳转。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0331-点击成员头像`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：讨论组设置页成员头像/昵称整体为 Pressable，点击跳转 `/friend/friend-card` 并传入成员 `accountId`；好友场景下 `app/friend/friend-card.tsx` 标题为“好友名片”。运行时阻塞：当前终端无可交互设备，无法实际点击头像确认页面。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0332-标记`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 的“标记”行点击后跳转 `/chat/pins` 并传入 `conversationId`；`app/chat/pins.tsx` 页面标题为“标记消息”。运行时阻塞：当前终端无可交互设备，无法实际点击确认页面。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0333-历史记录`                                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 的“历史记录”行点击后跳转 `/chat/history`，传入 `conversationId` 和讨论组标题；`app/chat/history.tsx` 加载当前会话历史消息并展示历史记录页。运行时阻塞：当前终端无可交互设备，无法实际点击确认页面。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0334-开启消息提醒`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 中“开启消息提醒”开关值为 `!conversation?.mute`；打开开关调用 `conversationStore.toggleMute(conversationId, false)`；`ConversationStore` 对 TEAM 会话调用 `setTeamMessageMuteMode(...OFF)`，语义为取消免打扰/开启提醒。运行时阻塞：当前终端无可交互设备和真实群消息，无法验证实际提醒到达。                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0335-关闭消息提醒`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：关闭“开启消息提醒”时调用 `conversationStore.toggleMute(conversationId, true)`；`ConversationStore` 对 TEAM 会话调用 `setTeamMessageMuteMode(...ON)`，语义为开启免打扰；会话列表将 `mute` 映射为 `muted`，`UIKitConversationRow` 展示 `icon-xiaoximiandarao`。运行时阻塞：当前终端无可交互设备和真实群消息，无法验证实际无提醒。                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0336-聊天置顶开`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：讨论组设置页“聊天置顶”打开时调用云会话 `stickTopConversationActive(conversationId, true)` 或本地 `conversationStore.toggleStickTop(conversationId, true)`；会话排序逻辑对 `stickTop=true` 优先展示。运行时阻塞：当前终端无可交互设备，无法实际观察讨论组会话列表置顶。                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0337-聊天置顶关`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：讨论组设置页“聊天置顶”关闭时调用云会话 `stickTopConversationActive(conversationId, false)` 或本地 `conversationStore.toggleStickTop(conversationId, false)`；刷新后 `stickTop=false` 不再参与置顶优先排序。运行时阻塞：当前终端无可交互设备，无法实际观察讨论组会话列表取消置顶。                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `0338-所有成员退出讨论组`                                        | RUNTIME-BLOCKED                     | 代码核验：`app/_layout.tsx` 监听 `onTeamLeft`、`onTeamMemberLeft`、`onTeamDismissed` 并刷新群列表；`app/chat/[id].tsx` 监听当前群 `onTeamDismissed/onTeamLeft` 并提示当前群不可用。运行时阻塞：用例要求所有成员分别退出后服务端自动解散讨论组，必须有真实多人账号、多端/多设备和服务端事件验证，当前终端无法执行。                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0339-讨论组最后一个非数字人成员退出讨论组`                      | RUNTIME-BLOCKED                     | 代码核验：创建/邀请/退出路径存在，数字人通过选择器加入；退出和解散事件由 `app/_layout.tsx`、`app/chat/[id].tsx` 监听刷新。运行时阻塞：预期“最后一个非数字人成员退出后自动解散，数字人不能作为群主”属于服务端成员角色/数字人规则，需要真实账号、数字人配置和多人依次退出验证，当前终端无法执行。                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `0340-无网络退出讨论组`                                          | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 在确认退出前调用 `ensureNetworkAvailable()`，断网时不会执行 `leaveTeam`、删除会话或跳转，仍停留在设置页；`utils/network.ts` 文案改为“当前网络不可用，请检查你的网络设置”。验证：`npx prettier --check app/team/settings.tsx utils/network.ts` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无法切换设备网络并点击确认弹窗做实机验证。                                                                                                                                                                                                                                                                                                                                                                                  |
| `0341-切换网络/断网重连退出讨论组`                               | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：退出讨论组成功后从 `/contacts/teamlist` 改为 `/(tabs)`，回到会话列表页；原逻辑已调用 `teamStore.leaveTeam()` 并 `conversationStore.deleteConversation(conversationId, true)` 删除该会话。验证：`npx prettier --check app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无法切换网络/断网重连并实际退出讨论组。                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0342-断网操作聊天置顶or免打扰开关`                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 的消息提醒和聊天置顶开关操作前均调用 `ensureNetworkAvailable()`；断网时提示 `当前网络不可用，请检查你的网络设置`，不会继续调用 SDK 设置接口。验证：`npx prettier --check app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无法断开设备网络并实际操作开关。                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0343-反复操作聊天置顶or免打扰开关`                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 为消息提醒和聊天置顶分别增加 `savingMute`、`savingStickTop` 提交中保护，快速反复触发时忽略并发操作；原逻辑已有 try/catch 错误提示。验证：`npx prettier --check app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无法做实机连续点击稳定性验证。                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0344-退出讨论组`                                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：二次确认弹窗文案调整为标题“退出讨论组”、内容“确认退出讨论组？”，按钮为“退出”和“取消”；确认后执行网络检测、`teamStore.leaveTeam()`、删除会话并返回 `/(tabs)` 会话列表；取消按钮使用系统 cancel 行为，停留在设置页。验证：`npx prettier --check app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无可交互设备，无法实际点击退出/取消。                                                                                                                                                                                                                                                                                                                                                                                        |
| `0345-讨论组设置页面成员有人退出-加入群聊，群成员列表不实时更新` | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 在当前设置页监听 `onTeamInfoUpdated`、`onTeamMemberJoined`、`onTeamMemberKicked`、`onTeamMemberLeft`，事件属于当前 `teamId` 时重新执行 `loadTeamDetails()`，实时刷新成员数量和预览列表；重新进入页面本身也会加载详情。验证：`npx prettier --write app/team/settings.tsx && npx tsc --noEmit` 通过。运行时阻塞：当前终端无真实多人群事件，无法实机确认成员加入/退出后的实时刷新。                                                                                                                                                                                                                                                                                                                                             |
| `0346-群组设置页UI`                                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`stores/TeamStore.ts` 新增群/讨论组类型标记，`app/conversation/picker.tsx` 仅在单聊邀请创建路径写入讨论组标记，未标记团队按普通群处理；`app/team/settings.tsx` 普通群显示“群名称/群头像”，成员加号按邀请权限展示，保留“标记”“历史记录”“开启消息提醒”“聊天置顶”，群主/管理员展示“群管理”，仅群主展示“群禁言”，群主底部动作为“解散群聊”，普通成员为“退出群聊”；讨论组仍保留前置用例要求的讨论组文案和退出动作。验证：`npx prettier --check app/team/settings.tsx app/conversation/picker.tsx stores/TeamStore.ts stores/index.ts` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实群主/管理员/普通成员账号和可交互设备，无法截图核验各角色 UI。                                      |
| `0347-标题显示`                                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 在加载态、失败态和正常页面均设置 `<Stack.Screen options={{ title: '设置', headerTitleAlign: 'center' }} />`，群组设置页标题居中显示“设置”。验证：`npx prettier --check app/team/settings.tsx` 通过。运行时阻塞：当前终端无可交互设备，无法实际进入群组设置页截图核验标题。                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0348-群名称`                                                    | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 顶部群名称增加 `numberOfLines={1}` 和 `ellipsizeMode="tail"`，超长群名称单行省略；群名称设置行继续展示 `team?.name`，并由 UIKit 信息行限制文本行数防止撑开布局。验证：`npx prettier --check app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无可交互设备，无法实际创建超长群名称并截图确认省略号。                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0349-群设置页展示群号`                                          | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 顶部信息区在群名称下方渲染 `群号：{resolvedTeamId                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |     | '-'}`，满足群号展示在群名称下的预期。运行时阻塞：当前终端无可交互设备，无法实际进入群设置页截图确认。 |
| `0350-点击群名称右边箭头`                                        | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：新增 `app/team/info.tsx` 群信息页，标题为“群信息”，展示群头像、群名称、群介绍三项并保留进入头像/名称/介绍编辑页的箭头；`app/team/settings.tsx` 群名称行点击从直接编辑名称改为进入 `/team/info`。验证：`npx prettier --check app/team/settings.tsx app/team/info.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备，无法实际点击箭头并截图确认群信息页。                                                                                                                                                                                                                                                                                                                  |
| `0351-群成员左侧"+"`                                             | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/member-picker.tsx` 邀请成员前增加 `ensureNetworkAvailable()`，网络不可用时提示统一文案并不继续调用邀请接口。代码核验通过：`app/team/settings.tsx` 仅在 `canInviteMembers` 为真时展示成员横排左侧“+”，点击进入 `/team/member-picker`；选择页过滤自己、已在群成员和黑名单好友，空态显示“暂无可邀请好友/未找到可邀请好友”，确认后调用 `teamStore.inviteMembers()` 且 SDK `inviteMember` 不要求被邀请者同意。验证：`npx prettier --check app/team/member-picker.tsx app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过。运行时阻塞：当前终端无真实好友/群权限数据和可交互设备，无法实际点击加号、邀请好友或截图空态。                                                                                                                        |
| `0352-群成员头像`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 成员预览列表中每个成员头像/昵称包裹在 `Pressable` 内，点击跳转 `/friend/friend-card` 并传入该成员 `accountId`；`app/friend/friend-card.tsx` 根据关系展示“好友名片/陌生人名片”，可作为成员个人名片页。运行时阻塞：当前终端无可交互设备，无法实际点击成员 B 并确认页面跳转。                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0353-开启消息提醒-开关`                                         | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` “开启消息提醒”开关值为 `!conversation?.mute`，打开时调用 `conversationStore.toggleMute(conversationId, false)`，关闭时调用 `conversationStore.toggleMute(conversationId, true)`；`stores/ConversationStore.ts` 对 TEAM 会话调用 `setTeamMessageMuteMode(...OFF/ON)` 后刷新会话；会话列表将 `mute` 映射到 `UIKitConversationRow.muted`，关闭提醒时展示 `icon-xiaoximiandarao` 免打扰标识。前置修复已覆盖断网检测和并发保护。运行时阻塞：当前终端无真实群消息、系统通知和可交互设备，无法验证新消息提醒实际到达或关闭。                                                                                                                                                                                                  |
| `0354-聊天置顶`                                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` “聊天置顶”开关值为 `!!conversation?.stickTop`，打开/关闭分别调用云会话 `stickTopConversationActive(conversationId, true/false)` 或本地 `conversationStore.toggleStickTop(conversationId, true/false)`；`stores/ConversationStore.ts` 刷新后按 `stickTop` 优先排序；会话列表把 `stickTop` 传给 `UIKitConversationRow.pinned` 并展示 `icon-xiaoxizhiding`，关闭后不再置顶。前置修复已覆盖断网检测和并发保护。运行时阻塞：当前终端无可交互设备，无法实际切换开关并返回会话列表观察排序。                                                                                                                                                                                                                                  |
| `0355-群禁言开关默认状态`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：新建群聊时 `stores/TeamStore.ts` 未主动设置禁言模式，服务端默认应为未禁言；`app/team/settings.tsx` 仅当 `team?.chatBannedMode === V2NIM_TEAM_CHAT_BANNED_MODE_BANNED_NORMAL` 时群禁言开关为开，其余 undefined/UNBAN 状态均显示关闭，符合新群默认关闭。运行时阻塞：当前终端无真实创建群聊流程和群主账号，无法实机确认新群默认开关状态。                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0356-群禁言`                                                    | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/chat/[id].tsx` 根据当前群 `chatBannedMode` 和本人角色计算普通成员禁言态；禁言时输入框占位文案显示“当前群主设置为禁言”，`TextInput` 不可编辑，发送、语音、表情、图片和更多按钮均不响应，扩展面板按钮禁用；群主/管理员不受禁言限制。设置页开关仍由群主调用 `teamStore.updateTeamInfo()` 写入 BANNED_NORMAL/UNBAN。验证：`npx prettier --check app/chat/[id].tsx app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实群主/管理员/普通成员多账号和可交互设备，无法实机验证键盘不拉起和消息功能按钮无反应。                                                                                                                                                     |
| `0357-群禁言@数字人`                                             | RUNTIME-BLOCKED                     | 代码核验：`0356` 修复只在禁言态限制普通成员输入，群主/管理员仍可正常输入发送，不阻断“群主或管理员 @ 数字人”的前提；当前 RN 聊天页未实现独立的群 @/数字人消息组装 UI，数字人已在群内时回复失败并展示 tip、非群成员数字人正常回复属于服务端/SDK 数字人响应链路。运行时阻塞：当前终端无数字人配置、真实群主/管理员账号、群内/群外数字人对照数据和可交互设备，无法实际 @ 数字人并确认 tip。                                                                                                                                                                                                                                                                                                                                                                      |
| `0358-群聊输入框输入文字后未发送，群禁言开启`                    | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/chat/[id].tsx` 在普通成员进入群禁言态时清空未发送 `inputText` 和回复草稿，关闭文本/表情/更多面板并 `Keyboard.dismiss()`；禁言期间输入框 value 为空，因此显示“当前群主设置为禁言”，解除禁言后原未发送文案不保留。验证：`npx prettier --check app/chat/[id].tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实普通成员账号、群主远端开启禁言事件和可交互设备，无法实机确认键盘收起及解除禁言后的草稿状态。                                                                                                                                                                                                                                                                   |
| `0359-打开表情消息弹窗后群禁言开启`                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/chat/[id].tsx` 在 `isCurrentTeamChatBanned` 变为真时执行 `setEmojiPanelVisible(false)`、清空未发送文本和回复草稿，并显示“当前群主设置为禁言”；禁言态下表情按钮 `disabled`，`handleOpenEmoji()` 和 `handleEmojiPress()` 均直接返回，解除禁言后原输入文案不保留。运行时阻塞：当前终端无真实普通成员账号、远端开启禁言事件和可交互设备，无法实际打开表情面板后观察其被收起。                                                                                                                                                                                                                                                                                                                                                                 |
| `0360-打开本地文件/图片选择器后群禁言开启`                       | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/chat/[id].tsx` 禁言态前可打开图片/文件选择器；如果选择器打开后远端开启禁言，选择器返回后 `handlePickMedia()`/`handlePickFile()` 仍会调用 `messageStore.sendImageMessage()`/`sendVideoMessage()`/`sendFileMessage()`，不会在本地吞掉消息；服务端禁言拒绝后应由 `MessageStore.performSend()` 进入发送失败状态，其他端不会收到。禁言态生效后，更多/图片/文件入口会被禁用并关闭扩展面板。运行时阻塞：该用例必须在系统选择器保持打开时由群主远端开启禁言，并依赖服务端返回禁言失败，当前终端无法验证失败态和其他端收不到。                                                                                                                                                                                                                     |
| `0361-无网络退出/解散群聊`                                       | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 退出/解散确认按钮先调用 `ensureNetworkAvailable()`；断网时抛出 `当前网络不可用，请检查你的网络设置`，进入 catch 弹出“操作失败”提示，不调用 `teamStore.leaveTeam()`/`dismissTeam()`、不删除会话、不跳转，因此停留在群设置页；确认弹窗本身由系统关闭。运行时阻塞：当前终端无法切换设备网络并实际点击确认验证提示和停留页面。                                                                                                                                                                                                                                                                                                                                                                                             |
| `0362-切换网络/断网重连退出or解散群聊`                           | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：网络恢复后确认按钮会通过 `ensureNetworkAvailable()`；群主普通群调用 `teamStore.dismissTeam()`，其他成员/讨论组调用 `teamStore.leaveTeam()`；成功后调用 `conversationStore.deleteConversation(conversationId, true)` 删除会话并 `router.replace('/(tabs)')` 回到会话列表页。运行时阻塞：当前终端无法模拟设备切网/断网重连并实机确认会话列表删除。                                                                                                                                                                                                                                                                                                                                                                                               |
| `0363-解散群聊`                                                  | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/chat/[id].tsx` 对当前群 `onTeamDismissed` 事件提示从泛化文案调整为“该群聊已解散”，确认后删除会话并回到 `/(tabs)`；设置页群主底部动作显示“解散群聊”，二次确认标题为“解散群聊”、内容“确认解散群聊？”，确认后调用 `teamStore.dismissTeam()`、删除当前会话并回会话列表，取消按钮停留当前页。全局 `app/_layout.tsx` 已监听 `onTeamDismissed` 刷新群和会话列表。验证：`npx prettier --check app/chat/[id].tsx app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实多成员群和可交互设备，无法验证所有成员端弹窗、群数据清理和设置页并发操作时的服务端“群组不存在”提示。                                                                                           |
| `0364-退出群聊`                                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：普通群员 `isOwner=false` 且非讨论组时，`app/team/settings.tsx` 底部动作显示“退出群聊”，二次确认标题“退出群聊”、内容“确认退出群聊？”，确认按钮“退出”；确认后调用 `teamStore.leaveTeam()`，本地删除 team/members，随后 `conversationStore.deleteConversation(conversationId, true)` 删除会话并 `router.replace('/(tabs)')` 回到会话列表；取消按钮为系统 cancel，留在当前页。全局 `app/_layout.tsx` 监听 `onTeamLeft` 刷新“我的群聊”列表。运行时阻塞：当前终端无普通群员真实账号和可交互设备，无法实机确认会话列表及通讯录我的群聊列表删除。                                                                                                                                                                                                      |
| `0365-群成员达到上限邀请成员`                                    | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/settings.tsx` 使用 `team.memberLimit/memberCount` 判断群成员是否已达上限，满员时不展示顶部和成员横排的邀请“+”；`app/team/member-picker.tsx` 不在选择过程中限制超限选择，而是在点击“确定”时判断 `memberCount + selectedIds.length > memberLimit`，提示“群成员已达上限”并停留在选择页。验证：`npx prettier --check app/team/settings.tsx app/team/member-picker.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无接近上限的大群数据和可交互设备，无法实机确认满员隐藏加号及超限提示。                                                                                                                                                                                      |
| `0366-群设置页面成员有人退出/加入群聊，群成员列表实时更新`       | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 进入页面时调用 `loadTeamDetails()` 刷新群信息和成员；页面驻留期间监听当前 `teamId` 的 `onTeamInfoUpdated`、`onTeamMemberJoined`、`onTeamMemberKicked`、`onTeamMemberLeft`，命中当前群后重新加载详情，成员人数和预览列表会刷新；离开聊天页再进入设置页也会重新加载详情。运行时阻塞：当前终端无真实多人群成员加入/退出事件和可交互设备，无法实机观察实时刷新。                                                                                                                                                                                                                                                                                                                                                           |
| `0367-断网/离线状态对方解散群聊/被移除群聊`                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/_layout.tsx` 在登录状态恢复和本地会话同步完成时执行 `refreshAppState()`，刷新会话列表、好友、已加入群列表；收到 `onTeamDismissed/onTeamLeft/onTeamMemberKicked` 等事件时刷新群列表和会话列表。若 v2 本地会话仍残留，用户手动进入聊天页时 `app/chat/[id].tsx` 会 `teamStore.refreshTeamInfo(targetId)`，失败后弹出异常提示，确认后删除该会话并回会话列表。运行时阻塞：该用例依赖断网/退出登录期间其他端解散或移除本人，再联网/登录后的服务端同步行为，当前终端无法实机验证列表是否自动清理或点击残留会话后的弹窗。                                                                                                                                                                                                                         |
| `0368-群成员列表页UI`                                            | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/members.tsx` 使用 Stack 默认返回入口，标题为“群成员”且居中；页面展示搜索框和 `FlatList` 群成员列表，成员行包含头像、昵称、账号和角色信息，空态/加载失败态也有对应展示。运行时阻塞：当前终端无可交互设备和真实群成员数据，无法截图核验 UI。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0369-返回<`                                                     | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群设置页通过 `router.push('/team/members')` 进入成员列表页；`app/team/members.tsx` 没有自定义覆盖 headerLeft，使用 Expo Router/Stack 默认返回按钮，点击返回会回到上一页。运行时阻塞：当前终端无可交互设备，无法实际点击返回验证。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `0370-点击群成员头像`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 成员行点击时区分账号；点击其他成员跳转 `/friend/friend-card?accountId=...` 打开成员个人名片，点击自己则跳转 `/user/my-detail` 打开个人信息页。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备，无法实际点击成员 B 和自己头像确认页面。                                                                                                                                                                                                                                                                                                                                                             |
| `0371-群成员列表成员昵称展示顺序`                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/members.tsx` 成员昵称使用 `getUIKitAppellation({ account, teamId })`；`src/NEUIKit/rn/identity.ts` 的兜底顺序为好友备注 `friend.alias` > 群昵称 `teamMember.teamNick` > 用户昵称 `user.name/selfProfile.name/friend.userProfile.name` > 消息昵称 > accid，符合“好友备注 > 群昵称 > 好友个人昵称 > accid”。运行时阻塞：当前终端无具备备注/群昵称/个人昵称组合的真实成员数据，无法实机逐项截图确认。                                                                                                                                                                                                                                                                                                                                   |
| `0372-查看群成员头像`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`src/NEUIKit/rn/identity.ts` 的头像兜底 label 不再复用包含群昵称的 `getUIKitAppellation()`，改为好友备注 > 好友/用户昵称 > accid，避免无头像时取群昵称末尾两位；头像图片仍通过 `UIKitUserAvatar` 优先展示传入的好友个人头像。验证：`npx prettier --check src/NEUIKit/rn/identity.ts app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无不同头像/备注/群昵称组合的真实成员数据，无法实机逐项截图确认。                                                                                                                                                                                                                                                               |
| `0373-群成员列表排序`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 成员排序从单纯按 `joinTime` 改为群主优先，非群主成员再按入群时间正序；成员行继续展示头像、昵称和角色文本，群主行显示“群主”标识。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实多成员群和可交互设备，无法截图确认排序。                                                                                                                                                                                                                                                                                                                                                                                  |
| `0374-群成员列表页他人加入/退出群聊`                             | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 在成员列表页监听当前群的 `onTeamMemberJoined`、`onTeamMemberKicked`、`onTeamMemberLeft`，事件成员属于当前 `teamId` 时重新加载成员列表；退出到群设置页时，`app/team/settings.tsx` 也已有同类监听并会刷新成员人数和预览列表；重新进入成员列表页会再次加载成员。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无真实多人群加入/退出事件和可交互设备，无法实机观察列表刷新。                                                                                                                                                                                                                                      |
| `0375-进入群成员列表的数字人占群成员人数`                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/member-picker.tsx` 邀请超限判断使用 `team.memberCount` 或当前成员列表长度，没有排除数字人；候选列表虽可展示数字人，但已在群成员集合中的数字人会计入 `existingMemberIds` 并从可邀请候选中过滤；因此 1998 个真实成员 + 1 个数字人时，邀请 2 人会按 1999+2 超过上限提示，邀请 1 人可继续调用邀请。运行时阻塞：当前终端无 2000 人上限群、1998 真实成员和数字人 A 的真实数据，无法实机确认服务端上限与提示。                                                                                                                                                                                                                                                                                                                              |
| `0376-群人数大于200人群聊，成员展示完整`                         | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`stores/TeamStore.ts` 的 `loadMembers()` 从只拉取第一页 100 人改为使用 `nextToken/finished` 循环分页拉取所有群成员；`app/team/members.tsx` 和设置页都复用该加载逻辑，因此 20/200/2000 人群均可拿到完整成员数据后展示。验证：`npx prettier --check stores/TeamStore.ts app/team/members.tsx app/team/settings.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无 200/2000 人真实群，无法实机确认完整展示性能和分页结果。                                                                                                                                                                                                                                                             |
| `0377-搜索框占位文案`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 群成员列表搜索框占位文案从“搜索群成员”调整为“搜索”。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备，无法截图确认占位文案。                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0378-删除搜索内容`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/members.tsx` 的 `filteredMembers` 根据 `keyword.trim().toLowerCase()` 过滤；搜索“1”时只展示名称或账号包含 1 的成员；`UIKitSearchBar` 在有输入时展示清除按钮，点击调用 `onChangeText('')`，成员列表回到无关键字状态并展示所有成员。运行时阻塞：当前终端无可交互设备和真实成员数据，无法实际输入/清除搜索内容。                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0379-搜索无匹配结果`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 搜索无匹配结果时的空态文案从“未找到匹配成员”调整为“暂无结果”。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备和真实成员数据，无法实际输入 `com` 并截图确认空态。                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0380-搜索群成员`                                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 搜索匹配从“可见昵称或 accid”改为仅按当前可见昵称连续模糊匹配，符合所见即所得；排序改为群主优先、管理员优先于普通成员、同角色内按加入时间正序。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无具备昵称/群昵称/好友备注组合和管理员角色的真实群数据，无法实机验证搜索命中与排序。                                                                                                                                                                                                                                                                                                                              |
| `0381-一键清除按钮`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`UIKitSearchBar` 在 `value` 非空时展示一键清除按钮，点击后调用 `onChangeText('')`；成员列表页 `keyword` 清空后 `filteredMembers` 回到默认无搜索状态，展示默认成员列表。运行时阻塞：当前终端无可交互设备，无法实际输入“1”并点击清除按钮观察默认状态。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0382-群主进入群成员列表`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群主角色满足 `canManage=true`；成员行 `canKick` 对群主视角会给除自己和群主外的成员展示“移除”按钮，普通成员和管理员均可被群主移除。运行时阻塞：当前终端无真实群主账号和多角色成员数据，无法实机确认每行按钮展示。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0383-管理员进入群成员列表`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：管理员角色满足 `canManage=true`；成员行 `canKick` 排除自己、群主，并在当前用户为管理员时排除其他管理员，因此管理员视角仅普通成员展示“移除”按钮。运行时阻塞：当前终端无真实管理员账号和多角色成员数据，无法实机确认按钮展示。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0384-普通成员进入群成员列表`                                    | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：普通成员角色 `canManage=false`，成员行 `canKick` 恒为 false，因此所有群成员后面都不展示“移除”按钮；普通成员也没有右上角邀请入口。运行时阻塞：当前终端无普通成员真实账号和可交互设备，无法实机确认按钮隐藏。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0385-群管理员移除群成员`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：管理员视角 `canKick` 对群主和其他管理员为 false，对普通成员 C 为 true；点击移除后弹出确认，确认调用 `teamStore.kickMembers(teamId, [accountId])`，成功后重新 `loadMembers()` 并 `refreshTeamInfo()`，成员列表随 store 更新。运行时阻塞：当前终端无管理员/管理员 B/普通成员 C 的真实群数据和可交互设备，无法实机移除并确认列表刷新。                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0386-移除群成员`                                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`utils/teamNotification.ts` 的 TEAM_KICK 通知文案从“被移出群组”调整为“被移出群聊”，并继续通过 `getUIKitAppellation({ account, teamId })` 生成被移除成员名，优先级为备注 > 群昵称 > 个人昵称 > accid。代码核验通过：成员列表点击移除后调用 `teamStore.kickMembers()`，成功后重新加载成员并刷新群信息，成员 A 会从列表消失。验证：`npx prettier --check utils/teamNotification.ts app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy` 通过。运行时阻塞：当前终端无真实群成员和可交互设备，无法实机移除并观察通知消息。                                                                                                                                                                       |
| `0387-移除群成员二次确认`                                        | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 移除成员二次确认弹窗调整为标题“是否移除”、内容“移除后该成员将离开当前群聊”，按钮为“移除”和“取消”；点击取消不调用 `teamStore.kickMembers()`，成员仍留在列表，点击移除才执行踢人并刷新列表。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无可交互设备，无法实际点击取消/移除确认弹窗行为。                                                                                                                                                                                                                                                                                                                     |
| `0388-移除多个群成员`                                            | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：当前 RN 成员列表按单成员逐次移除，`app/team/members.tsx` 每次确认调用 `teamStore.kickMembers(teamId, [accountId])`，成功后刷新成员列表；`utils/teamNotification.ts` TEAM_KICK 通知会按被移除成员名生成“xxx 被移出群聊”。多次移除多个成员时会分多次调用 SDK，服务端应分多次下发通知。运行时阻塞：当前终端无真实多成员群和可交互设备，无法连续移除多个成员并观察多条系统通知。                                                                                                                                                                                                                                                                                                                                                                   |
| `0389-移除数字人`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：成员列表移除权限只基于成员角色和当前用户角色判断，不排除数字人账号；数字人若是普通成员，群主或管理员可看到“移除”按钮并调用 `teamStore.kickMembers()`，成功后刷新成员列表。运行时阻塞：当前终端无已入群数字人和群主/管理员真实账号，无法实机确认移除成功。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0390-群主从群成员列表移除管理员`                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群主视角 `canManage=true`，`canKick` 仅排除自己和群主，不排除管理员，因此群主可移除管理员 A；移除成功后 `teamStore.kickMembers()` 重新加载成员并刷新群信息，通知文案已修为“xxx 被移出群聊”。A 再次加群是否恢复普通成员身份属于服务端角色规则。运行时阻塞：当前终端无群主/管理员 A 真实群数据和可交互设备，无法实机移除、重新邀请并确认角色。                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0391-管理员移除群成员过程被收回权限`                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 将移除成员接口返回的无权限错误码 `109306` 映射为提示“您暂无操作权限”；当前页面未监听 `onTeamMemberInfoUpdated` 角色变更事件，因此管理员权限被收回后，本页已有移除按钮不会仅因失败请求立即消失，下次重新进入会重新加载成员角色并隐藏按钮。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无管理员权限动态收回的真实多端场景，无法实机确认失败提示和下次进入按钮隐藏。                                                                                                                                                                                                                                           |
| `0392-在群成员列表被授予编辑群成员权限`                          | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 监听 `onTeamMemberInfoUpdated`，仅当当前用户从普通成员升级为管理员/群主时重新加载成员列表；刷新后 `canManage=true`，普通成员后展示移除按钮。该逻辑不处理降权刷新，避免破坏 `0391` 要求的当前页按钮不立即消失。验证：`npx prettier --check app/team/members.tsx` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy` 通过。运行时阻塞：当前终端无普通成员被实时授予管理员权限的真实多端场景，无法实机确认按钮即时出现。                                                                                                                                                                                                                                                                          |
| `0393-移除群成员时群成员离开群聊`                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 在移除成员接口返回成员已不存在/资源不存在时按幂等成功处理，不弹“移除失败”，并重新加载成员列表；保留无权限错误 `109306` 的“您暂无操作权限”提示。`utils/teamNotification.ts` 的 TEAM_LEAVE 通知文案调整为“xxx退出了群聊”，TEAM_KICK 保持“xxx 被移出群聊”，覆盖成员先退出与被先移除两个分支。验证：`npx prettier --check app/team/members.tsx utils/teamNotification.ts openspec/changes/align-team-notification-message-copy/specs/team-conversation-notifications/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无真实多端并发退出/移除场景，无法实机确认服务端先后顺序与聊天页通知到达。                          |
| `0394-多端登录，一端在群成员列表，另一端移除群成员`              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/members.tsx` 成员列表页驻留期间监听 `onTeamMemberKicked`，事件回调会检查被移除成员的 `teamId` 是否为当前群，命中后调用 `loadMembers()` 重新加载成员列表；因此 A 端停留在成员列表、B 端移除成员后，A 端列表会随 SDK 事件刷新。验证沿用 `0393` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无 A/B 双端登录、真实群成员和可交互设备，无法实机确认事件到达与列表刷新。                                                                                                                                                                                                                                                                                                                                                             |
| `0395-断网移除群成员`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 在确认移除后、调用 `teamStore.kickMembers()` 前执行 `ensureNetworkAvailable()`；断网时不会调用移除成员 SDK 操作，并提示“当前网络异常，请检查你的网络设置”。由于检测发生在系统确认按钮回调内，点击“移除”后原二次确认弹窗会先关闭，再展示网络异常提示。验证：`npx prettier --check app/team/members.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无法切换设备网络并实机点击确认弹窗。                                                                                                                                                      |
| `0396-移除普通成员时，普通成员被设为管理员`                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：移除先发起成功时，`teamStore.kickMembers()` 成功后重新 `loadMembers()` 并刷新群信息，成员 A 不再出现在列表；若成员 A 先被设为管理员，当前管理员仍可点击当前页已有移除入口发起请求，SDK 会按服务端权限返回失败，`app/team/members.tsx` catch 分支弹出标题“移除失败”的提示。当前页只对“当前用户升管”监听 `onTeamMemberInfoUpdated` 并刷新，不会因目标成员升管立即隐藏已有移除按钮，符合本用例的竞态操作入口。验证沿用 `0395` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无真实多人群、管理员角色变更和并发操作环境，无法实机确认服务端先后返回。                                                                                                                                                                                          |
| `0397-群头像设置页UI`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 的头像编辑态标题固定为“修改头像”，左侧显示“取消”并返回上一页，右侧从“完成”调整为“保存”；当前群头像改为可点击头像区域并叠加拍摄相机 icon；默认头像区域保留“选择默认头像”和 `DEFAULT_TEAM_AVATARS` 的五个默认群头像选项。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无可交互设备，无法截图确认头像页 UI。                                                                                                                                                                                         |
| `0398-取消`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 头像编辑页左侧“取消”只执行 `router.back()`；选择默认头像或相册图片只更新页面本地 `avatarUri` 状态，不会调用 `teamStore.updateTeamInfo()`，只有右侧“保存”才提交群头像更新。因此修改后取消、不修改直接取消都会返回上一页且群组头像不变。验证沿用 `0397` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无可交互设备，无法实机点击取消并观察群设置页头像。                                                                                                                                                                                                                                                                                                                                                                 |
| `0399-上传本地头像`                                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 点击头像上的拍摄相机 icon 时先展示底部半层页，选项为“拍摄”“从相册选择”“取消”；“拍摄”调用相机权限和 `ImagePicker.launchCameraAsync()`，“从相册选择”调用相册权限和 `ImagePicker.launchImageLibraryAsync()`，选择后只更新头像预览，仍需点击“保存”才提交。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无可交互设备和系统相机/相册弹窗，无法实机确认半层页与权限流程。                                                                                                                                |
| `0400-iOS从相册选择`                                             | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群头像页点击“从相册选择”后调用 `ensureMediaLibraryPermission()`；该函数使用 `ImagePicker.requestMediaLibraryPermissionsAsync()` 请求系统相册权限，iOS 未授权时会触发系统照片访问授权弹窗，后续允许完全访问或限制访问均按授权结果继续打开相册。运行时阻塞：当前终端无 iOS 设备/模拟器系统权限弹窗，无法实机确认“IM想访问你的照片”“限制访问”“允许完全访问”“不允许”等系统文案。                                                                                                                                                                                                                                                                                                                                                                   |
| `0401-iOS相册权限弹窗-允许`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`ensureMediaLibraryPermission()` 对 `permission.granted` 和 `permission.accessPrivileges === 'limited'` 都返回 true；授权通过后 `app/team/edit.tsx` 调用 `ImagePicker.launchImageLibraryAsync()` 打开系统相册选择器。完全访问时照片范围由 iOS 系统提供；限制访问时再次打开只能看到已授权照片也由 iOS limited photos 权限控制，业务代码未额外绕过系统权限。运行时阻塞：当前终端无 iOS 设备/模拟器和系统照片库，无法实机确认完全访问/限制访问后的照片可见范围。                                                                                                                                                                                                                                                                                  |
| `0402-iOS权限请求弹窗-拒绝`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：首次系统相册权限请求被拒绝时，`ensureMediaLibraryPermission()` 返回 false，`app/team/edit.tsx` 停留在当前头像编辑页且不打开相册；再次点击“从相册选择”时，如果系统返回 `canAskAgain=false`，`utils/permissions.ts` 会弹出“权限不足”提示，并提供“去设置”动作调用 `Linking.openSettings()`。运行时阻塞：当前终端无 iOS 设备/模拟器权限状态，无法实机确认拒绝后的系统状态和二次提示弹窗。                                                                                                                                                                                                                                                                                                                                                          |
| `0403-iOS拍摄`                                                   | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群头像页点击相机 icon 后选择“拍摄”，会执行 `handleTakePhoto()` 并调用 `ensureCameraPermission()`；该函数使用 `ImagePicker.requestCameraPermissionsAsync()` 请求系统相机权限，iOS 未授权时会触发“IM想访问你的相机”的系统授权弹窗，授权后才调用 `ImagePicker.launchCameraAsync()`。运行时阻塞：当前终端无 iOS 设备/模拟器和相机权限弹窗，无法实机确认“不允许/好”等系统按钮文案。                                                                                                                                                                                                                                                                                                                                                                 |
| `0404-iOS相机权限弹窗-允许`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`ensureCameraPermission()` 在相机权限允许时返回 true，随后 `app/team/edit.tsx` 的 `handleTakePhoto()` 调用 `ImagePicker.launchCameraAsync({ mediaTypes: ['images'], quality: 1 })`，进入系统拍照页面。运行时阻塞：当前终端无 iOS 设备/模拟器和相机硬件/权限状态，无法实机确认拍照页面打开。                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0405-iOS相机权限请求弹窗-拒绝`                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：首次相机权限请求被拒绝时，`ensureCameraPermission()` 返回 false，`app/team/edit.tsx` 停留在头像编辑页且不打开相机；再次点击“拍摄”时，如果系统返回 `canAskAgain=false`，`utils/permissions.ts` 会弹出“权限不足”提示，并提供“去设置”动作调用 `Linking.openSettings()`。运行时阻塞：当前终端无 iOS 设备/模拟器权限状态，无法实机确认拒绝后的系统状态和二次提示弹窗。                                                                                                                                                                                                                                                                                                                                                                              |
| `0406-android点击拍照`                                           | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群头像页点击相机 icon 后选择“拍摄”，`handleTakePhoto()` 会先调用 `ensureCameraPermission()`，该函数通过 `ImagePicker.requestCameraPermissionsAsync()` 进入平台相机权限请求流程；Android 未授权时由系统展示相机权限授权流程，授权通过后才调用 `ImagePicker.launchCameraAsync()`。运行时阻塞：当前终端无 Android 设备/模拟器，无法实机确认系统权限流程。                                                                                                                                                                                                                                                                                                                                                                                         |
| `0407-android点击从相册选择`                                     | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群头像页选择“从相册选择”后执行 `ensureMediaLibraryPermission()`，该函数调用 `ImagePicker.requestMediaLibraryPermissionsAsync()` 进入平台媒体库权限请求流程；Android 未授权时由系统展示存储/照片访问权限流程，授权通过后调用 `ImagePicker.launchImageLibraryAsync()`。运行时阻塞：当前终端无 Android 设备/模拟器，无法实机确认系统权限流程。                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0408-拍摄头相上传`                                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 拍摄头像时 `ImagePicker.launchCameraAsync()` 增加 `allowsEditing: true`，拍摄后进入系统图片编辑/裁剪流程；完成后先在头像编辑页更新本地预览，点击“保存”时若是本地 URI 则调用 `teamStore.updateTeamAvatar()` 上传文件并更新群头像，成功后返回上一页。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无相机、图片裁剪 UI 和真实群资料，无法实机确认拍摄裁剪与头像更新。                                                                                                                                |
| `0409-从相册选择选择头相上传`                                    | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 从相册选择头像时 `ImagePicker.launchImageLibraryAsync()` 增加 `allowsEditing: true`，选择图片后进入系统图片编辑/裁剪流程；完成后先在头像编辑页更新本地预览，点击“保存”时若是本地 URI 则调用 `teamStore.updateTeamAvatar()` 上传文件并更新群头像，成功后返回上一页。验证沿用 `0408` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无相册、图片裁剪 UI 和真实群资料，无法实机确认相册裁剪与头像更新。                                                                                                                                                                                                                                                                                                                          |
| `0410-更多半层页-取消`                                           | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 点击头像/相机 icon 后展示底部半层页，选项包含“拍摄”“从相册选择”“取消”；点击“取消”执行 `setAvatarSheetVisible(false)`，只关闭半层页，不触发相机、相册或保存，回到头像编辑页。运行时阻塞：当前终端无可交互设备，无法实机点击确认半层页关闭效果。                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0411-切换选择默认图标`                                          | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 默认头像列表使用 `DEFAULT_TEAM_AVATARS` 渲染五个选项；点击任一默认头像执行 `setAvatarUri(item)`，顶部当前群头像组件绑定同一个 `avatarUri`，因此选择默认图标 1、再选择默认图标 2 时，页面头像预览会跟随切换，选中态也随 `avatarUri === item` 更新。运行时阻塞：当前终端无可交互设备，无法实机点击确认预览变化。                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0412-上传头像后保存`                                            | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：选择默认头像保存时，`handleSaveAvatar()` 调用 `teamStore.updateTeamInfo(teamId, { avatar })`；拍摄或从相册选择本地图片保存时，先调用 `teamStore.updateTeamAvatar()` 上传文件并更新群头像；成功后返回上一页。群聊设置页、群信息页、修改头像页均从 `teamStore.getTeam(teamId)?.avatar` 读取头像，因此保存成功后会展示最新头像；未修改直接保存时写回相同头像值，群图像不变。验证沿用 `0408` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无真实群、NOS 上传环境和可交互设备，无法实机确认三处页面头像刷新。                                                                                                                                                                                                                                  |
| `0413-上传头像后不保存返回上一页`                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 中选择默认头像、拍摄图片、从相册选择图片都只更新本地 `avatarUri` 预览；只有右侧“保存”会调用 `teamStore.updateTeamInfo()` 或 `teamStore.updateTeamAvatar()`。左侧“取消”和系统返回仅离开页面，不提交头像变更，因此群组头像保持修改前头像；未修改保存也写回相同头像值，群图像不变。运行时阻塞：当前终端无可交互设备，无法实机确认返回后设置页头像未变。                                                                                                                                                                                                                                                                                                                                                                       |
| `0414-无网络保存群头像`                                          | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 的 `handleSaveAvatar()` 在提交头像前先调用 `ensureNetworkAvailable()`；断网时抛出 `NETWORK_UNAVAILABLE_MESSAGE`，catch 分支弹出“保存失败”提示并显示“当前网络不可用，请检查你的网络设置”，不会调用头像更新接口，也不会执行 `router.back()`，因此停留在当前页面。运行时阻塞：当前终端无法切换设备网络并实机点击保存。                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0415-切换网络/断网重连保存群头像`                               | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：头像页保存时每次都会重新调用 `ensureNetworkAvailable()` 获取当前网络状态；断网恢复或切换网络后再次点击保存，会继续执行默认头像更新或本地图片上传更新，成功后 `router.back()` 返回上一页，群头像由 `teamStore` 中最新 `team.avatar` 展示。运行时阻塞：当前终端无法切换设备网络、重连并实机确认保存成功。                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0416-无网络上传群头像`                                          | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 选择相册图片或拍摄照片完成后不再提前做网络检测，只更新本地头像预览；点击“保存”时再执行 `ensureNetworkAvailable()` 和上传/更新。断网保存时提示“当前网络不可用，请检查你的网络设置”，停留在头像编辑页且保留已选预览，网络恢复后可继续保存。说明：用例正文写“回到上一页”，备注写“只有提示，没有返回上一页，网络连接之后可以继续保存”，本次按备注实现和记录。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无法断网、拍摄/选图并实机确认提示与重试。                                                   |
| `0417-切换网络/断网重连上传群头像`                               | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：拍摄/相册选择完成后本地头像预览保存在 `avatarUri`；切换网络或断网重连后点击“保存”，`handleSaveAvatar()` 会重新检测当前网络，在线时对本地 URI 调用 `teamStore.updateTeamAvatar()` 上传并更新群头像，成功后返回上一页。验证沿用 `0416` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无法切换网络/断网重连并实机确认头像上传成功。                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0418-多人同时修改群头像`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：头像保存逻辑每次都调用服务端更新群资料，默认头像走 `teamStore.updateTeamInfo({ avatar })`，本地图片走 `teamStore.updateTeamAvatar()` 上传后更新；本地没有做旧值冲突校验或覆盖保护，因此多人同时修改时最终群头像由服务端最后一次成功保存的头像决定。说明：用例步骤写“修改群名称”，但标题和预期均为群头像，本次按群头像核验。运行时阻塞：当前终端无 A/B 多端和真实群资料，无法实机确认服务端最后写入者生效。                                                                                                                                                                                                                                                                                                                                     |
| `0419-在会话列表他人修改群头像`                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/_layout.tsx` 监听 `onTeamInfoUpdated` 并调用 `teamStore.refreshJoinedTeams()` 刷新群资料；会话列表 `app/(tabs)/index.tsx` 通过 `getUIKitConversationIdentity()` 解析头像，TEAM 会话会从 `conversation.avatar` 或 `teamStore.getTeam(targetId)?.avatar` 读取最新群头像。聊天通知文案由 `utils/teamNotification.ts` 渲染，群头像变更时返回“XX 更新了群头像”。运行时阻塞：当前终端无他人多端修改头像和可交互设备，无法实机确认会话列表头像刷新及通知到达。                                                                                                                                                                                                                                                                                   |
| `0420-在群聊设置页他人修改群头像`                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 设置页进入时调用 `loadTeamDetails()` 刷新群信息，页面驻留期间监听当前群的 `onTeamInfoUpdated`，命中当前 `teamId` 后重新加载详情；设置页顶部头像和“群头像/讨论组图像”行都绑定 `team?.avatar`，因此他人修改群头像后会动态更新，离开后再次进入也会重新拉取最新头像。运行时阻塞：当前终端无他人多端修改头像事件和可交互设备，无法实机确认设置页动态刷新。                                                                                                                                                                                                                                                                                                                                                                  |
| `0421-无权限修改群头像进入头像设置页面`                          | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 根据 `team.updateInfoMode` 和当前成员角色判断群资料编辑权限；无权限进入头像页时只展示当前群头像，不展示右上“保存”、拍摄相机 icon、底部拍摄/相册半层入口和默认头像选项。`app/team/info.tsx` 的头像入口也按同一权限隐藏箭头和点击跳转，避免无权限用户从群信息页进入编辑态。验证：`npx prettier --check app/team/edit.tsx app/team/info.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无无权限成员真实账号和可交互设备，无法截图确认只读 UI。                                                                                                   |
| `0422-群名称输入框字符最大限制30位`                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 群名称编辑继续使用 `maxLength=30`，29/30 位可输入，31 位会被输入框拒绝不展示；群名称输入框调整为多行输入并提供两行高度，长名称编辑时可换行展示。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无可交互设备，无法实机输入 29/30/31 位字符并截图确认换行。                                                                                                                                                                                                                                           |
| `0423-群名称输入框展示已输入字符数`                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群名称编辑页底部字符计数使用 `{value.length}/{config.maxLength}`；名称最大长度为 30，输入 3 位显示 `3/30`，输入 30 位显示 `30/30`，继续输入第 31 位会被 `maxLength=30` 拦截，计数仍为 `30/30`。运行时阻塞：当前终端无可交互设备，无法实机输入并截图确认计数。                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `0424-输入框一键清除按钮x`                                       | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群名称编辑页输入框 `value` 非空时渲染清除按钮 `×`；点击清除按钮执行 `setValue('')`，输入内容和字符计数会同步清空。运行时阻塞：当前终端无可交互设备，无法实机输入 `IMdemo` 并点击清除按钮确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0425-取消`                                                      | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 文本编辑页左侧新增“取消”，点击只执行 `router.back()`，不会调用 `handleSave()` 或 `teamStore.updateTeamInfo()`；因此修改群名称后取消、未修改直接取消都会返回群设置页且群名称不变。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无可交互设备，无法实机点击取消确认。                                                                                                                                                                                                                                |
| `0426-修改群名称保存`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 群名称非空保存时调用 `teamStore.updateTeamInfo({ name: value.trim() })` 并返回上一页；不修改保存会写回同名值，展示不变；删除群名称后 `canSaveTextField=false`，右侧“完成”置灰且点击不会提交。群名称/群介绍无编辑权限时不显示保存按钮。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无真实群和可交互设备，无法实机保存并观察设置页名称。                                                                                                                                                           |
| `0427-输入空格保存群名称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群名称保存可用性使用 `value.trim().length > 0` 判断；输入全空格时右侧“完成”置灰且 `handleSave()` 直接返回，不会提交全空格群名称。说明：用例预期正文写保存失败并提示“不支持全空格”，备注写 Android 保存按钮不可点击；当前 RN 行为按备注实现为不可点击。运行时阻塞：当前终端无可交互设备，无法实机输入全空格并确认按钮状态。                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0428-无网络修改群组名称`                                        | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 文本字段保存前统一调用 `ensureNetworkAvailable()`；断网保存群名称时 catch 分支弹出“保存失败”并显示“当前网络不可用，请检查你的网络设置”，不会提交群名称更新，也不会返回上一页，因此停留在当前编辑页。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无法断开设备网络并实机点击保存。                                                                                                                                                                                                                 |
| `0429-断网重连他人修改群组名称`                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/_layout.tsx` 在会话同步完成、登录状态恢复和 AppState 回前台时执行 `refreshAppState()`，其中会调用 `teamStore.refreshJoinedTeams()` 拉取最新群资料；在线收到 `onTeamInfoUpdated` 也会刷新群列表。群设置页进入时 `loadTeamDetails()` 会重新拉取当前群信息，驻留时也监听当前群 `onTeamInfoUpdated` 并刷新，因此断网重连后各页面再次刷新可展示他人修改后的群名称。运行时阻塞：当前终端无法模拟真实离线期间他人修改和重连后的 SDK 同步。                                                                                                                                                                                                                                                                                                       |
| `0430-断网重连/切换网络修改群组名称`                             | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群名称编辑页保存时会重新调用 `ensureNetworkAvailable()` 检测当前网络；断网重连或切换网络恢复后点击保存，会调用 `teamStore.updateTeamInfo({ name })` 更新群名称并 `router.back()` 返回群设置页，设置页绑定 `team?.name` 展示修改后的群组名称。运行时阻塞：当前终端无法切换网络/断网重连并实机确认保存成功。                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0431-多人同时修改群名称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群名称保存直接调用 `teamStore.updateTeamInfo({ name })` 更新服务端群资料，本地没有旧值冲突校验或版本拦截；多人同时修改时，每个端的保存请求由服务端按到达/成功顺序处理，最终群名称以最后一次成功保存的信息为准。运行时阻塞：当前终端无 A/B 多端和真实群资料，无法实机确认最后保存者生效。                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0432-在会话列表他人修改群名称`                                  | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/(tabs)/index.tsx` 会话列表对通知消息预览统一显示 `[通知消息]`，不再展开具体群通知文案；群名称更新仍由 `app/_layout.tsx` 的 `onTeamInfoUpdated` 监听触发 `teamStore.refreshJoinedTeams()`，会话列表标题通过 `getUIKitConversationIdentity()` 使用最新群资料展示。聊天页通知正文仍由 `utils/teamNotification.ts` 渲染具体“XX 更新了群名称…”文案。验证：`npx prettier --check app/(tabs)/index.tsx openspec/changes/align-team-notification-message-copy/specs/team-conversation-notifications/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无他人多端修改群名称和可交互设备，无法实机确认会话列表更新。                              |
| `0433-在群聊设置页他人修改群名称`                                | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`utils/teamNotification.ts` 群名称更新通知文案调整为“XX修改群名称为xxx”。代码核验通过：`app/team/settings.tsx` 驻留期间监听当前群 `onTeamInfoUpdated` 并刷新详情，设置页群名称会变更为最新名称；聊天页通知消息体继续通过 `getTeamNotificationText()` 展示具体群名称修改通知。验证：`npx prettier --check utils/teamNotification.ts openspec/changes/align-team-notification-message-copy/specs/team-conversation-notifications/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无他人多端修改群名称事件和可交互设备，无法实机确认设置页与聊天页通知。                                                                                      |
| `0434-在群聊聊天页面他人修改群名称`                              | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/chat/[id].tsx` 当前群聊天页监听 `onTeamInfoUpdated`，命中当前群后刷新群资料，聊天页标题随 `teamStore.getTeam(targetId)?.name` 更新；`utils/teamNotification.ts` 已将群名称变更通知文案调整为“XX修改群名称为xxx”。验证：`npx prettier --check app/chat/[id].tsx openspec/changes/align-team-notification-message-copy/specs/team-conversation-notifications/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-team-notification-message-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无他人多端修改群名称事件和可交互设备，无法实机确认聊天页标题与通知。                                                                                                                                                       |
| `0435-群介绍设置页UI`                                            | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/edit.tsx` 群介绍等文本编辑页右侧动作从“完成”调整为“保存”；页面左侧显示“取消”，标题使用路由传入的“群介绍”并居中，输入区域复用多行文本框；群名称/群介绍无群资料编辑权限时不显示保存按钮。验证：`npx prettier --check app/team/edit.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无可交互设备，无法截图确认 UI。                                                                                                                                                                                                                                         |
| `0436-群介绍输入框字符最大限制100位`                             | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 的 `fieldConfig.intro.maxLength` 为 100，群介绍输入框传入 `maxLength={100}`；输入 99/100 位可成功展示，继续输入第 101 位会被输入框限制，不再追加到 `value`。运行时阻塞：当前终端无可交互设备，无法实机输入 99/100/101 位字符确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0437-群介绍输入框展示已输入字符数`                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群介绍编辑页底部字符计数使用 `{value.length}/{config.maxLength}`；群介绍最大长度为 100，输入 3 位显示 `3/100`，输入 100 位显示 `100/100`。运行时阻塞：当前终端无可交互设备，无法实机输入并截图确认计数。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0438-输入框一键清除按钮x`                                       | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群介绍编辑页复用文本编辑输入框，`value` 非空时渲染清除按钮 `×`；点击清除按钮执行 `setValue('')`，群介绍输入内容和字符计数会同步清空。运行时阻塞：当前终端无可交互设备，无法实机输入“测试文案”并点击清除按钮确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0439-取消`                                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群介绍编辑页左侧“取消”只执行 `router.back()`，不会调用 `handleSave()` 或 `teamStore.updateTeamInfo()`；因此修改介绍后取消、未修改直接取消都会返回群设置页且群介绍不变。运行时阻塞：当前终端无可交互设备，无法实机点击取消确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0440-保存群介绍`                                                | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群介绍保存时调用 `teamStore.updateTeamInfo(resolvedTeamId, { intro: value.trim() })`，成功后 `router.back()` 返回群设置页，设置页“群介绍”行绑定 `team?.intro` 显示修改后内容；不修改直接保存会写回相同介绍，显示不变。运行时阻塞：当前终端无真实群和可交互设备，无法实机保存并观察设置页介绍。                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0441-输入空格保存群介绍`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 群介绍保存不做全空格禁用校验，提交前使用 `value.trim()`；输入全空格保存会调用 `teamStore.updateTeamInfo({ intro: '' })`，成功后群介绍显示为空，符合备注“不校验全空格”。运行时阻塞：当前终端无真实群和可交互设备，无法实机保存全空格介绍。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0442-无网络修改群组介绍`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 文本字段保存前统一调用 `ensureNetworkAvailable()`；断网保存群介绍时 catch 分支弹出“保存失败”并显示“当前网络不可用，请检查你的网络设置”，不会提交群介绍更新，也不会返回上一页，因此停留在当前编辑页。验证沿用 `0428` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无法断开设备网络并实机点击保存。                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `0443-断网重连修改群组介绍`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群介绍编辑页保存时会重新调用 `ensureNetworkAvailable()` 检测当前网络；断网重连恢复后点击保存，会调用 `teamStore.updateTeamInfo({ intro })` 更新群介绍并 `router.back()` 返回群设置页，设置页绑定 `team?.intro` 展示修改后的群介绍。运行时阻塞：当前终端无法断网重连并实机确认保存成功。                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0444-切换网络修改群组介绍`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群介绍编辑页保存时会重新调用 `ensureNetworkAvailable()` 检测当前网络；切换网络后点击保存，在线时调用 `teamStore.updateTeamInfo({ intro })` 更新群介绍并返回群设置页，设置页展示修改后的群介绍。运行时阻塞：当前终端无法切换设备网络并实机确认保存成功。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0445-多人同时修改群介绍`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：群介绍保存直接调用 `teamStore.updateTeamInfo({ intro })` 更新服务端群资料，本地没有旧值冲突校验或版本拦截；多人同时修改时，每个端的保存请求由服务端按到达/成功顺序处理，最终群介绍以最后一次成功保存的信息为准。运行时阻塞：当前终端无 A/B 多端和真实群资料，无法实机确认最后保存者生效。                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0446-我在群里的昵称`                                            | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 的“我的群昵称”行点击后跳转 `/team/edit`，参数为 `field=teamNick`、`title=我在群里的昵称`，进入本人群昵称设置页面。运行时阻塞：当前终端无可交互设备，无法实机点击确认跳转。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0447-我在群里的昵称的页面标题`                                  | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/settings.tsx` 跳转本人群昵称设置页时传入 `title=我在群里的昵称`；`app/team/edit.tsx` 使用该标题配置 `Stack.Screen` 且 `headerTitleAlign: 'center'`，页面标题居中显示“我在群里的昵称”。运行时阻塞：当前终端无可交互设备，无法截图确认标题。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0448-左上角"取消"`                                              | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 本人群昵称设置页左侧“取消”只执行 `router.back()`，不会调用 `handleSave()` 或 `teamStore.updateMyNick()`；因此返回上一页群设置页且不保存输入框内容。运行时阻塞：当前终端无可交互设备，无法实机点击取消确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0449-文本输入框字符种类限制`                                    | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 本人群昵称输入框没有对字符种类做过滤，仅传入 `maxLength=30`；普通文案“大大111”和特殊字符“@#=-\_?”都会进入 `value` 并正常展示。运行时阻塞：当前终端无可交互设备，无法实机输入确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0450-设置我在群里的昵称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 本人群昵称保存调用 `teamStore.updateMyNick()`；`stores/TeamStore.ts` 保存成功后重新 `loadMembers(teamId)`，群设置页“我的群昵称”和聊天消息展示都能读取最新成员昵称。昵称展示通过 `getUIKitAppellation({ account, teamId })`，优先级为好友备注 > 群昵称 > 个人昵称/消息昵称 > accid，因此有好友备注时仍优先展示备注，无备注时展示新的群昵称。运行时阻塞：当前终端无真实群、多端接收端和可交互设备，无法实机确认发送/不发送消息后的接收端展示。                                                                                                                                                                                                                                                                               |
| `0451-删除我在群里的昵称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：本人群昵称输入框清空后点击保存会调用 `teamStore.updateMyNick(teamId, '')`，保存成功后重新加载成员；`getUIKitAppellation({ account, teamId })` 在群昵称为空时回退到个人昵称，且好友备注仍优先于群昵称/个人昵称，因此接收端展示符合“个人昵称，有备注优先展示备注”。运行时阻塞：当前终端无真实群、多端接收端和可交互设备，无法实机确认发送/不发送消息后的展示。                                                                                                                                                                                                                                                                                                                                                                                   |
| `0452-输入框"x"`                                                 | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 本人群昵称设置页输入框 `value` 非空时渲染清除按钮 `×`；点击清除按钮执行 `setValue('')`，输入框内容清空。运行时阻塞：当前终端无可交互设备，无法实机输入并点击清除按钮确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0453-输入框字符长度验证`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 的 `fieldConfig.teamNick.maxLength` 为 30，本人群昵称输入框传入 `maxLength={30}`；输入超过 30 个字符后，超出部分不会进入输入框。运行时阻塞：当前终端无可交互设备，无法实机输入超长字符确认。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0454-输入空格保存群昵称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：本人群昵称保存时调用 `teamStore.updateMyNick(resolvedTeamId, value.trim())`；输入全空格时 `trim()` 后为空字符串，保存成功后等同未设置群昵称，显示名回退到个人昵称且好友备注仍优先。运行时阻塞：当前终端无真实群和可交互设备，无法实机保存空格昵称。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0455-无网络保存群昵称`                                          | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：`app/team/edit.tsx` 文本字段保存前统一调用 `ensureNetworkAvailable()`；断网保存本人群昵称时 catch 分支弹出“保存失败”并显示“当前网络不可用，请检查你的网络设置”，不会提交群昵称更新，也不会返回上一页，因此停留在当前编辑页。验证沿用 `0428` 后的 `npx tsc --noEmit` 通过。运行时阻塞：当前终端无法断开设备网络并实机点击保存。                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0456-断网重连修改群组名称`                                      | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：该用例与 `0430` 场景重复；群名称编辑页保存时重新调用 `ensureNetworkAvailable()`，断网重连恢复后点击保存会调用 `teamStore.updateTeamInfo({ name })` 更新群名称并返回群设置页，设置页展示修改后的群组名称。运行时阻塞：当前终端无法断网重连并实机确认保存成功。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0457-切换网络修改群昵称`                                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：本人群昵称编辑页保存时重新调用 `ensureNetworkAvailable()`；切换网络后在线保存会调用 `teamStore.updateMyNick()`，成功后重新加载群成员并返回群设置页。聊天消息显示名通过 `getUIKitAppellation()` 解析，优先好友备注，再群昵称，因此无备注时接收端可看到新的群昵称，有备注时仍优先展示备注。运行时阻塞：当前终端无法切换网络、发送消息并在接收端实机确认昵称展示。                                                                                                                                                                                                                                                                                                                                                                                |
| `0458-设置我在群里的昵称后查看群成员列表`                        | CODE-PASS / RUNTIME-BLOCKED         | 代码核验通过：本人群昵称保存成功后 `teamStore.updateMyNick()` 会重新加载群成员；`app/team/members.tsx` 成员列表昵称使用 `getUIKitAppellation({ account, teamId })` 展示，优先级为好友备注 > 群昵称 > 个人昵称/消息昵称 > accid，因此自己查看时可看到自己的群昵称，他人查看时无备注展示该群昵称、有备注优先展示备注。运行时阻塞：当前终端无真实群和他人端，无法实机确认两端成员列表展示。                                                                                                                                                                                                                                                                                                                                                                     |
| `0459-在群成员列表页有人更新自己的群昵称`                        | FIXED / CODE-PASS / RUNTIME-BLOCKED | 已修复：`app/team/members.tsx` 的 `onTeamMemberInfoUpdated` 监听现在会在当前群其他成员信息更新时刷新成员列表；当前用户自身群昵称等信息更新且角色未变时也刷新成员列表；仍保留当前用户被降权时不立即刷新，避免破坏 `0391` 的权限竞态要求。验证：`npx prettier --check app/team/members.tsx openspec/changes/align-discussion-settings-copy/specs/discussion-settings/spec.md` 通过，`npx tsc --noEmit` 通过，`openspec validate align-discussion-settings-copy --type change --no-interactive` 通过。运行时阻塞：当前终端无真实群成员昵称更新事件和可交互设备，无法实机确认动态刷新。                                                                                                                                                                          |

### 未执行用例

- 无。

### 后续要求

- 若需要全仓 lint 通过，还需修复其他模块既有格式/导入排序问题。
- 使用可交互 Expo 环境或真机/模拟器执行 `npm run start`、`npm run ios` 或 `npm run android`，确认 Metro ready 后再逐条手动验证。
- 执行该用例集时必须串行执行；每条失败后先修复并复测同一条，通过后再继续下一条。
