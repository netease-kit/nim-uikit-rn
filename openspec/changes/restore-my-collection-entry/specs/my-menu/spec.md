## ADDED Requirements

### Requirement: My page includes the collection menu item

The "我的" page MUST render a `收藏` menu row in its primary menu section so the page matches the current workbook validation path.

#### Scenario: Viewing the My primary menu

- **WHEN** the user opens the `我的` tab
- **THEN** the primary menu shows `关于云信`、`设置`、and `收藏`
- **AND** the `收藏` row is tappable from the same menu section
