## Why

聊天页面最后一条消息被撤回后，会话列表的外漏文案显示为 `【未知消息体】`，与实际消息状态不符。用户期望在会话列表直接看到撤回结果提示 `此消息已撤回`。

## What Changes

- 调整首页会话列表最后一条消息预览文案生成逻辑。
- 当最后一条消息处于撤回状态时，优先显示撤回提示文案。
- 保持其它消息类型的预览规则不变。

## Impact

- 影响 `app/(tabs)/index.tsx` 的会话预览文本计算。
- 不改变消息撤回流程与会话排序逻辑。
