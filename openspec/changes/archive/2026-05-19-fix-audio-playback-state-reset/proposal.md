## Why

聊天页播放语音消息后，部分设备在播放完成后仍然保持“播放中”状态，导致语音动画不停止。当前实现只依赖单一完成事件字段，设备差异下不够稳定。

## What Changes

- 调整聊天页语音播放结束判定逻辑。
- 在播放完成、自然结束或异常终止时统一重置播放状态。
- 保持手动点击停止语音时的状态清理行为一致。
- 将 RN 端语音录制格式对齐为 AAC/M4A，减少与 Android 端语音资源格式差异带来的播放兼容问题。

## Impact

- 影响 `app/chat/[id].tsx` 的语音播放状态管理。
- 不改变语音消息展示样式与播放入口。
