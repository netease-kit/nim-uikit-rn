## 1. Spec Alignment

- [ ] 1.1 Record the workbook gender options and back-to-save interaction.

## 2. Implementation And Validation

- [ ] 2.1 Update `app/user/gender.tsx` to show `未知 / 男 / 女` and default to `未知` when unset.
- [ ] 2.2 Save gender changes on back navigation and keep the page open with an error when the network is unavailable.
- [ ] 2.3 Validate `0023-0028` against the updated gender flow.
