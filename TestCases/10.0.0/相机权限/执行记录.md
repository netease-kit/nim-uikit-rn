# 相机权限测试用例执行记录

## 执行范围

- 用例目录：`TestCases/10.0.0/相机权限`
- 已执行范围：`0844` 至 `0850`
- 执行方式：按 `AGENTS.md` 要求一次只验证一条用例；当前环境无 Detox/Maestro 等自动化 runner，采用“读取用例 -> 核对实现路径 -> 必要代码修复 -> targeted lint/类型检查 -> 记录结果”的顺序。
- 环境限制：本套件依赖 Android/iOS 真机或可交互模拟器、系统相机权限弹窗、系统拍照页面和“仅本次允许/禁止且不再询问”等平台原生选项。当前终端已打通 Android development build 启动，但仍无法直接读取设备屏幕结果，因此系统弹窗与系统相机界面需要人工在真机上继续确认。

## 验证命令基线

- `npx eslint utils/permissions.ts`：通过。
- `npx tsc --noEmit`：通过。
- `npm run android`：已通过。2026-05-15 已在连接的 Android 真机 `PKB110` 上完成构建、安装 APK、启动 Metro，并自动打开 development build。
- 已知仓库非本次改动问题：`npx eslint app/user/my-detail.tsx` 仍有既有 Prettier error 和 1 个未使用变量 warning，本轮未改该文件。

## 用例执行明细

| 用例                                  | 结果                                       | 执行记录                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0844 首次请求相机权限                 | 通过（修复后代码通过，系统弹窗需复测）     | 已核对聊天页 `app/chat/[id].tsx`、群头像页 `app/team/edit.tsx`、个人头像页 `app/user/my-detail.tsx` 的拍摄入口都统一调用 `ensureCameraPermission()`。本轮已修复 `utils/permissions.ts`：首次未授权时先调用 `getCameraPermissionsAsync()` 判断状态；若尚未拒绝，则进入 `requestCameraPermissionsAsync()`，由系统展示相机权限请求弹窗，不再提前弹业务提示。                                          |
| 0845 相机权限-使用中允许              | 通过（代码通过，打开系统相机需复测）       | 当前 `ensureCameraPermission()` 在系统相机权限允许后返回 `true`；聊天页 `handleTakePhoto()` / `handleRecordVideo()`、群头像页 `handleTakePhoto()`、个人头像页 `handleTakePhoto()` 随后都会调用 `ImagePicker.launchCameraAsync(...)` 打开系统拍摄页面，符合“授权成功后进入相机拍摄页面”的预期。                                                                                                     |
| 0846 相机授权页面-禁止                | 通过（修复后代码通过，二次点击提示需复测） | 已修复首次拒绝后的顺序：首次在系统权限弹窗中点击“禁止”后，`requestCameraPermissionsAsync()` 返回未授权，业务侧仅停留当前页且不再弹自定义“请在设置页面添加相机权限”；再次点击拍摄时，`getCameraPermissionsAsync()` 已返回 `status === 'denied'`，这时才弹出“权限不足 / 请在设置页面添加相机权限”，并提供“去设置”动作。该顺序与用例要求一致，也兼容部分 Android 机型再次进入系统请求流程的系统差异。 |
| 0847 相机授权-仅本次运行允许          | 通过（代码通过，杀进程后系统行为需复测）   | 当前实现每次点击拍摄前都会实时读取 `getCameraPermissionsAsync()`；如果系统把“仅本次运行允许”在应用杀进程后恢复为未授权状态，则下次点击会再次进入 `requestCameraPermissionsAsync()` 系统请求链路，不会缓存旧授权结果。是否在当前 Android 真机上确实再次弹出权限请求，仍需按用例实机操作确认。                                                                                                       |
| 0848 再次请求相机权限                 | 通过（代码通过，系统弹窗样式需复测）       | 当前实现支持“再次请求相机权限”场景：当系统返回当前仍可请求时，点击拍摄会再次进入 `requestCameraPermissionsAsync()`；系统展示“允许 / 禁止且不在询问”等选项由机型和系统版本决定，业务代码不会拦截或覆盖系统弹窗。                                                                                                                                                                                    |
| 0849 再次请求相机权限 -禁止且不在询问 | 通过（代码通过，真机弹窗需复测）           | 当系统权限状态已进入拒绝且不可继续原生询问的状态时，当前 `ensureCameraPermission()` 会在下一次点击拍摄时走 `status === 'denied'` 分支，直接弹出“请在设置页面添加相机权限”，并提供 `Linking.openSettings()` 跳转入口，满足该用例的预期。                                                                                                                                                            |
| 0850 再次请求相机权限 -允许           | 通过（代码通过，打开系统相机需复测）       | 当再次请求相机权限时用户点击“允许”，`requestCameraPermissionsAsync()` 返回授权成功，`ensureCameraPermission()` 立即返回 `true`；后续拍摄入口会调用 `ImagePicker.launchCameraAsync(...)` 打开系统拍摄页面，满足“授权成功后可以打开相机”的预期。                                                                                                                                                     |
