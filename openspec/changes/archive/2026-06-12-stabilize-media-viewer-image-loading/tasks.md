## 1. Implementation

- [x] 1.1 Inspect the current RN media viewer image list, image source selection, and loading overlay behavior.
- [x] 1.2 Stabilize the image viewer layout so valid images render reliably without collapsing into a black screen.
- [x] 1.3 Reuse image-ready state for previously loaded images so the viewer does not show loading every time it reopens.

## 2. Validation

- [x] 2.1 Run `npx tsc --noEmit`.
- [x] 2.2 Run `npm run lint`.
