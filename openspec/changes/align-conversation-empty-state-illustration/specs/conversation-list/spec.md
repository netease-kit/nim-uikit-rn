## MODIFIED Requirements

### Requirement: Conversation List Empty State

The app SHALL show a clear empty state when the conversation list has no available conversations.

#### Scenario: No conversations available

- **WHEN** the user opens the conversation list and there are no conversations
- **THEN** the app MUST display an empty-state illustration
- **AND** the app MUST display the title `暂无会话`
