## 1. Spec Alignment

- [x] 1.1 Record that the conversation search page should follow the shared RN UIKit search-page pattern used by related pages.

## 2. Implementation And Verification

- [x] 2.1 Refactor `app/conversation/search.tsx` to reuse the shared RN UIKit search page structure and aligned spacing.
- [x] 2.2 Remove the create-group entry from the conversation search page top bar.
- [ ] 2.2 Verify the updated page with repository checks required for a route-level UI change.
