## MODIFIED Requirements

### Requirement: Blacklist Header Add Action Consistency

The app SHALL keep the blacklist-page add action visually consistent with the contacts-page add action.

#### Scenario: Render blacklist header right action

- **WHEN** the blacklist page renders its header-right add entry
- **THEN** the page MUST use the same add icon asset as the contacts page
- **AND** the button hit area MUST match the contacts-page header add action sizing
