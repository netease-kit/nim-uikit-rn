## ADDED Requirements

### Requirement: Conversation header icons must align with Android

The RN conversation page header MUST use search and add entry icons that match the Android conversation page implementation.

#### Scenario: Viewing the conversation page header

- **WHEN** the user views the conversation page header
- **THEN** the search entry icon matches the Android conversation page search icon
- **AND** the add entry icon matches the Android conversation page add icon
