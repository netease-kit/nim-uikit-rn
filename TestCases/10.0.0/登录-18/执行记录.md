# 登录-18 测试用例执行记录

## 执行范围

- 用例目录：`TestCases/10.0.0/登录-18`
- 执行方式：按 `AGENTS.md` 要求一次只验证一条用例；当前环境无 Detox/Maestro 等自动化 runner，采用“读取用例 -> 核对实现路径 -> 必要代码修复 -> 定向校验/启动验证 -> 记录结果”的顺序。
- 环境限制：涉及真实短信验证码、已注册/未注册手机号、验证码过期、验证码次数上限、多端登录、互踢、真机键盘与系统浏览器跳转保留态的用例，当前终端只能做静态与启动层验证；端到端结果需在已配置测试账号和真实设备环境复测。

## 验证命令基线

- `npm run start -- --non-interactive`：已执行；当前 Expo 55 输出 `--non-interactive is not supported, use $CI=1 instead`，项目继续启动。Metro ready 状态仍需后续终端输出确认。
- `npx tsc --noEmit`：通过。
- `npx eslint app/login.tsx stores/AuthStore.ts app/_layout.tsx`：无 error；存在仓库既有 warning：`app/_layout.tsx` 的 `react-hooks/exhaustive-deps` 依赖声明提示，与本次登录修复无新增行为错误。
- `OPENSPEC_TELEMETRY=0 openspec validate align-login-18-auth-flow --type change --no-interactive`：通过。

## 用例执行明细

| 用例                                           | 结果                                 | 执行记录                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0790 自动登录-成功                             | 通过（静态验证，真机登录态需复测）   | 已修复 `stores/AuthStore.ts`、`app/_layout.tsx`、`app/login.tsx`：persisted session 现在必须先完成 `nimStore.login(..., forceMode=false)` 校验，`authStore.hasValidatedSession` 为真后才进入 authenticated shell；符合“有效 token 自动登录成功”预期。真实 15 天 token 仍需测试环境复测。 |
| 0791 自动登录-失败                             | 通过（静态验证，过期 token 需复测）  | 已修复自动登录失败路径：`restoreNimLogin()` 失败后会 `clearPersistedSession()`，路由守卫只认 `hasValidatedSession`，无效或过期 token 不再提前进入首页，而是回到 `/home`/`/login` 登录前置页。                                                                                            |
| 0792 手动退出登录                              | 通过（静态验证，端到端需复测）       | 已核对 `app/user/setting.tsx` 调用 `authStore.logout()`，`logout()` 会清本地 session 并触发 `nimStore.logout()`；再次进入时不会满足 `hasValidatedSession`，因此不会自动登录。手动退出后的真实重启流程需设备复测。                                                                        |
| 0793 验证码登录页面元素检查                    | 通过（静态验证）                     | 已修复并核对 `app/login.tsx`：页面展示 `验证码登录`、未注册手机号提示 `未注册的手机号验证码通过后将自动注册`、手机号输入框、验证码输入框、获取验证码按钮、登录按钮。                                                                                                                     |
| 0794 手机号输入框                              | 通过（静态验证）                     | 已核对登录页展示 `+86`、手机号输入框占位文案 `请输入手机号`，且仅在 `mobile` 非空时展示 X 清除按钮。                                                                                                                                                                                     |
| 0795 手机号输入框-X按钮                        | 通过（静态验证）                     | 已核对 `app/login.tsx`：输入手机号后显示 X，点击后调用 `handleMobileChange('')`，输入内容清空，恢复占位文案 `请输入手机号`，X 按钮消失。                                                                                                                                                 |
| 0796 手机号输入框输入规范                      | 通过（静态验证）                     | 已修复 `handleMobileChange()` 为 `replace(/\\D/g, '').slice(0, 11)`；当前仅保留数字，最多 11 位，汉字/字母/符号都会被过滤，超长输入无效。                                                                                                                                                |
| 0797 验证码输入框                              | 通过（静态验证）                     | 已核对验证码输入框占位文案为 `请输入验证码`，右侧展示获取验证码按钮。                                                                                                                                                                                                                    |
| 0798 验证码输入框输入规范                      | 通过（静态验证）                     | 已修复 `handleSmsCodeChange()` 与 `AuthStore.validateSmsCode()`；验证码输入当前仅保留数字，最多 6 位，汉字/字母/符号输入无效。                                                                                                                                                           |
| 0799 正确手机号-获取验证码                     | 部分通过（真实短信需复测）           | 已核对 `AuthStore.requestSmsCode()` 成功后会 `startSmsCountdown()`，登录页按钮进入 60s 倒计时且 `disabled`；符合本地交互预期。手机是否收到短信验证码依赖真实号码与服务端环境，需复测。                                                                                                   |
| 0800 错误11位手机号-获取验证码                 | 通过（静态验证）                     | 已修复登录页本地校验：错误 11 位手机号点击获取验证码时直接 toast `请输入正确的手机号码`，不会发请求。                                                                                                                                                                                    |
| 0801 输入不足11位手机号                        | 通过（静态验证）                     | 已核对 10 位手机号不满足 `validateMobile()`，点击获取验证码时 toast `请输入正确的手机号码`。                                                                                                                                                                                             |
| 0802 不输入手机号-获取验证码                   | 通过（静态验证）                     | 已核对空手机号点击获取验证码时 toast `请输入正确的手机号码`。                                                                                                                                                                                                                            |
| 0803 验证码倒计时时长                          | 通过（静态验证）                     | 已核对 `startSmsCountdown()` 从 60 开始按秒递减，到 0 时恢复 `获取验证码`。                                                                                                                                                                                                              |
| 0804 再次获取验证码                            | 通过（静态验证，真实短信需复测）     | 已核对倒计时结束后 `smsCountdown === 0`，再次点击会重新请求并再次进入 60s 倒计时。真实二次短信下发需测试环境复测。                                                                                                                                                                       |
| 0805 验证码有效期时长10分钟                    | 阻塞（依赖真实短信内容）             | 当前仓库只负责发码与登录，不控制短信文案本身；“短信提示有效期10分钟”需要真实短信模板与服务端配置环境复测。                                                                                                                                                                               |
| 0806 验证码获取次数                            | 阻塞（依赖服务端限流策略）           | 用例明确“具体次数以服务端限制为准”。当前前端会透传服务端错误消息，但无法在本地静态验证“同一账号一天内超过 20 次”的实际限流行为。                                                                                                                                                         |
| 0807 验证码超限获取时长                        | 阻塞（依赖服务端限流策略）           | “超限后 24 小时可重新获取”属于服务端限流窗口行为，当前终端无法独立验证。                                                                                                                                                                                                                 |
| 0808 输入已注册手机号和正确验证码，登录成功    | 阻塞（依赖真实账号和短信）           | 代码路径已具备：`loginRegisterByCode()` 成功后执行 `nimStore.login()` 并持久化 session，再进入 authenticated shell。真实已注册手机号和正确验证码需测试环境复测。                                                                                                                         |
| 0809 输入已注册手机号及错误验证码，登陆失败    | 阻塞（依赖真实账号和服务端回包）     | 当前登录页已改为 toast 展示接口异常消息；若服务端返回“验证码错误”，前端会直接展示。真实错误验证码返回文案需测试环境复测。                                                                                                                                                                |
| 0810 不输入手机号-点击登录                     | 通过（静态验证）                     | 已修复登录前本地校验：空手机号点击登录直接 toast `请输入正确的手机号`。                                                                                                                                                                                                                  |
| 0811 不输入验证码-点击登录                     | 通过（静态验证）                     | 已修复登录前本地校验：手机号合法、验证码为空时点击登录直接 toast `请输入正确的验证码`。                                                                                                                                                                                                  |
| 0812 输入过期验证码-点击登录                   | 阻塞（依赖真实验证码时效）           | 当前前端会展示服务端返回的登录失败消息；过期验证码是否返回“请输入正确的验证码”依赖真实验证码时效和接口回包，需环境复测。                                                                                                                                                                 |
| 0813 输入未注册手机号和错误验证码              | 阻塞（依赖真实服务端回包）           | 当前前端会展示接口错误消息；未注册手机号配错误验证码的实际回包文案需真实环境复测。                                                                                                                                                                                                       |
| 0814 输入未注册手机号和正确验证码，点击登录    | 通过（静态验证，端到端需复测）       | 已核对 `requestSmsCode()` 返回 `isFirstRegister` 后，`submitLogin(false)` 会优先弹出温馨提示弹窗，不直接登录。真实未注册手机号与正确验证码需环境复测。                                                                                                                                   |
| 0815 未注册手机号-温馨提示弹窗                 | 通过（静态验证）                     | 已核对弹窗展示 `温馨提示` 标题、提示内容、`不同意` 按钮、`已阅读并同意` 按钮。                                                                                                                                                                                                           |
| 0816 未注册手机号-温馨提示弹窗 -点击服务协议   | 部分通过（系统跳转保留态需复测）     | 已核对弹窗内容内可点击《服务协议》，调用 `Linking.openURL()`，且当前代码不会主动关闭弹窗；返回后弹窗是否仍保留需设备复测。                                                                                                                                                               |
| 0817 未注册手机号-温馨提示弹窗 -点击隐私政策   | 部分通过（系统跳转保留态需复测）     | 已核对弹窗内容内可点击《隐私政策》，调用 `Linking.openURL()`，且当前代码不会主动关闭弹窗；返回后弹窗是否仍保留需设备复测。                                                                                                                                                               |
| 0818 未注册手机号-温馨提示弹窗 -点击弹窗外区域 | 通过（静态验证）                     | 已核对点击弹窗外层遮罩会 `setShowConsentModal(false)`，停留登录页且输入内容保留；再次点击登录时因 `awaitingRegistrationConfirmation` 仍为真，会再次弹出提示弹窗。                                                                                                                        |
| 0819 未注册手机号-温馨提示弹窗 -点击同意       | 通过（静态验证，真实注册登录需复测） | 已核对点击 `已阅读并同意` 后会执行 `submitLogin(true)`，绕过二次拦截直接进入注册/登录提交链路。真实注册成功需环境复测。                                                                                                                                                                  |
| 0820 未注册手机号-温馨提示弹窗 -点击不同意     | 通过（静态验证）                     | 已核对点击 `不同意` 只关闭弹窗，不清空手机号/验证码；再次点击登录会再次弹出提示弹窗。                                                                                                                                                                                                    |
| 0821 支持多端登录                              | 阻塞（依赖双端实测）                 | 当前前端登录成功后不会主动让已登录设备本地退出；但多端并存结果取决于 SDK/服务端多端策略，需双设备同账号实测。                                                                                                                                                                            |
| 0822 多端登录上限                              | 阻塞（依赖服务端策略与多端环境）     | “一个账号最多 10 个设备登录”属于服务端/SDK 端能力上限，当前终端无法独立验证。                                                                                                                                                                                                            |
| 0823 互踢                                      | 阻塞（依赖 11 端环境）               | “第 11 台设备登录踢掉最早登录设备”属于服务端多端策略；当前终端无法构造 11 端环境验证。                                                                                                                                                                                                   |

## 2026-05-17 续验：用户点名子集

| 用例                                           | 结果                               | 执行记录                                                                                                                                                                                                                                               |
| ---------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0790 自动登录-成功                             | 通过（静态验证，真实登录态需复测） | 复核 `AuthStore.bootstrap()` 会读取持久化 session，并通过 `restoreNimLogin()` 调用 `nimStore.login(saved.account, saved.token, false)` 校验 SDK 登录；成功后设置 `loginStatus = 1` 与 `hasValidatedSession = true`，路由守卫进入 authenticated shell。 |
| 0795 手机号输入框-X按钮                        | 通过（静态验证）                   | 复核 `app/login.tsx`：手机号非空时展示清除按钮，点击 `handleMobileChange('')` 后清空输入并隐藏按钮。                                                                                                                                                   |
| 0799 正确手机号-获取验证码                     | 部分通过（真实短信需复测）         | 复核手机号格式合法时 `handleRequestSmsCode()` 调用 `authStore.requestSmsCode()`；成功后启动 60s 倒计时并禁用重复点击。真实短信下发依赖服务端与测试手机号。                                                                                             |
| 0808 输入已注册手机号和正确验证码，登录成功    | 部分通过（真实账号/验证码需复测）  | 复核 `loginWithSms()` 成功路径会调用 `loginRegisterByCode()`、`nimStore.login(..., true)`、持久化 session，并跳转首页。真实已注册账号与正确验证码链路需服务端环境确认。                                                                                |
| 0809 输入已注册手机号及错误验证码，登陆失败    | 部分通过（服务端回包需复测）       | 复核登录页 catch 分支会展示接口错误信息且不会进入首页；“验证码错误”的具体文案取决于服务端回包。                                                                                                                                                        |
| 0811 不输入验证码-点击登录                     | 通过（静态验证）                   | 复核合法手机号但验证码为空时，`submitLogin()` 在本地 `validateSmsCode()` 前置失败并 toast `请输入正确的验证码`，不调用登录接口。                                                                                                                       |
| 0814 输入未注册手机号和正确验证码，点击登录    | 通过（静态验证，真实注册需复测）   | 复核发码返回 `isFirstRegister` 后会设置 `awaitingRegistrationConfirmation`，点击登录先弹出温馨提示，不直接注册登录。                                                                                                                                   |
| 0818 未注册手机号-温馨提示弹窗 -点击弹窗外区域 | 通过（静态验证）                   | 复核遮罩点击会关闭弹窗并停留登录页，手机号与验证码输入保留；再次登录会重新弹出温馨提示。                                                                                                                                                               |
| 0819 未注册手机号-温馨提示弹窗 -点击同意       | 通过（静态验证，真实注册需复测）   | 复核点击 `已阅读并同意` 后执行 `submitLogin(true)`，跳过二次弹窗进入注册/登录提交链路。                                                                                                                                                                |
| 0820 未注册手机号-温馨提示弹窗 -点击不同意     | 通过（静态验证）                   | 复核点击 `不同意` 只关闭弹窗，不清空输入；再次点击登录仍会弹出温馨提示。                                                                                                                                                                               |
| 0821 支持多端登录                              | 阻塞（依赖双端实测）               | 前端登录成功后没有主动踢出其他端；最终是否支持同账号多端并存取决于 NIM SDK/服务端策略，需要至少两台设备或模拟器组合复测。                                                                                                                              |
