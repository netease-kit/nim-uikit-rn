## 1. Implementation

- [x] 1.1 Remove the contacts home summary count strip from the header area.

## 2. Validation

- [x] 2.1 Run `npx tsc --noEmit`, `npm run lint`, and `OPENSPEC_TELEMETRY=0 openspec validate remove-contacts-summary-counts --type change --no-interactive`.
