## Overview

这次只调整 RN UIKit 聊天气泡组件的多选态点击分发，不改聊天页的选择态数据结构。

## Design

- 保留气泡主体 `Pressable` 在多选模式下执行 `onToggleSelect`
- 其它子交互入口在多选模式下直接短路返回，不触发跳转、打开链接、重发、回复源消息打开或其它动作
- 为富文本链接增加可覆盖点击回调，使聊天气泡能够在多选态吞掉链接点击而不影响非多选态默认打开链接

## Risks

- 聊天气泡中存在多个独立点击入口，需统一检查左右头像、回复引用、失败图标、链接和回执区域，避免遗漏
