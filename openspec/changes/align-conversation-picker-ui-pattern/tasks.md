## 1. Spec Alignment

- [x] 1.1 Record that the conversation picker page should follow the shared RN UIKit picker-page pattern used by related pages.

## 2. Implementation And Verification

- [x] 2.1 Refactor `app/conversation/picker.tsx` to reuse the shared RN UIKit picker page structure and aligned selection affordances.
- [x] 2.2 Verify the updated page with repository checks required for a route-level UI change.
