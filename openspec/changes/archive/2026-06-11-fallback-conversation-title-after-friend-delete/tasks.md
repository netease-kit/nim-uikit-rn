## 1. Implementation

- [x] 1.1 Prevent stale friend-remark fallback when resolving a P2P non-friend appellation.

## 2. Validation

- [x] 2.1 Run `npx tsc --noEmit`, `npm run lint`, and `OPENSPEC_TELEMETRY=0 openspec validate fallback-conversation-title-after-friend-delete --type change --no-interactive`.
