## 1. Spec Alignment

- [x] 1.1 Record that fresh installs default to local conversation mode until the user explicitly enables cloud conversation.

## 2. Implementation And Verification

- [x] 2.1 Update the default cloud-conversation config so fresh installs and missing preferences initialize in local conversation mode.
- [ ] 2.2 Verify the default-mode change with the required repository checks.
