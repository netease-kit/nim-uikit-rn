## 1. Implementation

- [x] 1.1 Prefer friend alias before team nickname in shared chat appellation resolution used by reply quote sender labels.

## 2. Validation

- [x] 2.1 Run `npx tsc --noEmit`, `npm run lint`, and `OPENSPEC_TELEMETRY=0 openspec validate prefer-alias-for-reply-quote-sender --type change --no-interactive`.
